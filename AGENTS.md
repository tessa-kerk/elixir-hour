# AGENTS.md

The full operating instructions for this repo live in **`CLAUDE.md`** (same directory) — read that first. It is the single source of truth for how to work in this repo.

This short `AGENTS.md` exists only so tools that follow the AGENTS.md convention (e.g. OpenAI Codex) get pointed at the same instructions, instead of keeping a second copy that drifts out of sync.
