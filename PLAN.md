# Elixir Hour — build plan & progress

## ⚡ CURRENT STATE — read this first (written 05-07-2026, pre-compaction)

**⛔ TERRITORY — two sessions run this repo in parallel (06-07-2026).** THIS (game) session owns **`game/` engine, data, assets, `captures/` — everything EXCEPT `site/`.** The **landing session owns `site/` entirely** — its HTML/CSS/JS, its og/twitter meta, its favicons, and the canonical-URL swap at deploy. The game's own `index.html` og:image/share meta stays with this session. **Never edit the other territory.** Need something in `site/`? Add a dated note under **"## Cross-session requests"** (below) and stop — don't touch the files. **Hygiene every work block:** `git pull` first; commit small and push immediately; stage only your own files (`git add <path>`, never `-A` while the other session has uncommitted work in the tree); if the tree holds changes you didn't make, read `git log` and leave them alone — flag, don't fix.

**WHERE WE ARE:** Milestones 0–7 done. M6 (05-07): Nights 2 & 3 verbatim, group/duo screens, the consequential brew → epilogue Herald, Ledger stages. **M7 (06-07):** the Night Cap share card (v2, composes on the parchment scroll); the per-Night light shift (§8, programmatic tint N1 warm → N2 dusk → N3 candlelight); the audio system (Songbook music channel + brew-fizz/mug-clink/page-turn SFX, pref-gated, graceful missing-file notes — real files drop later); canon terms follow the official Clash Royale localisation (§13, 42/48 verified, audit table below); the Showstopper dose-2 sync (v1.3). About/Credits + the 7-language string system were already in. Full 3-Night playthrough verified in-browser throughout.

**P0 (06-07) — fixed twice.** First pass: a stale-cached old `state.js` (sans `poured`) + new `dialogue.js` made `recordPour` throw, freezing the sequencer in `brew`. Fixed defensively (guarded `recordPour`, gate cleared before side effects + always `step()`, audio never throws, `onDone` before the clink) **and** systemically with `?v=<BUILD>` cache stamps + a blocked-storage cream toast. **REOPENED + fixed (06-07 round 2):** the "correct pour still stalls" report was reproduced as a UX trap, not a code stall — a **valid-but-wrong** pour (Elixir+Heal dose 2 = *The Easy One*) showed a ✓ and, on pour, silently re-showed the same request line, reading as frozen; the unlabelled dose slider made pouring 2-while-meaning-1 easy. Fixed per GDD §7: **(Fix 2) gate-aware ✓** (ticks ONLY the current order; valid-but-wrong shows the name, no tick), **(Fix 3) no silent rejection** (every wrong pour shows a visible line — the customer's bespoke `wrong:` line or a generic gentle beat — then the request stands), **(Fix 1) dose slider now shows drop pips + a live "N drop(s)" label**. Plus **(Fix 4)** Sage speaks from her own bottom-left sage-tinted bubble (no tail at the customer), and **(Fix F) a visible build stamp** in the title corner (`v9 · 06-07`, bumped with the `?v=` token — one source of truth in `strings.js window.BUILD`).

**CACHE-BUST / BUILD BUMP RITUAL (every commit):** bump `window.BUILD.n` in `strings.js` AND find-replace `?v=<old>`→`?v=<new>` across `index.html`, **`css/main.css`** and **`cap/index.html`** (the share page — added §R16; it loads the game's own `nightcap.js`, so a stale copy there breaks a shared link after a redeploy). Currently **`?v=35`**, BUILD n:35. The visible title stamp is the witness — if it doesn't match what you bumped, a build is stale.

**PROJECT MOVED (08-07):** the workshop is now at `Projects/Elixir Hour (Clash Royale)/` (out of the old `Projects/Supercell Job Search/…` subfolder — the consolidation step). Same git repo/remote (tessa-kerk/elixir-hour). The preview `.claude/launch.json` + a fresh scratchpad `serve.ps1` were repointed to the new path (the old ones 404'd; also added `.webp` to the server mime map).

**ROUND 20 (10-07) — TODAY'S EDITION: the live-ops seam, plugged in. Build v35.** R19 flagged that the case study promised a live "Today's Edition" that did not exist. It exists now. Tessa supplied the verified facts (Supercell release notes + RoyaleAPI, 10-07).
- **NEW `data/today.js`.** One `live` item + one `evergreen` fallback, nothing else. Season 85 *Honor & Exile* (06-07 → 03-08-2026): the lead is **Ronin, a wandering swordsman** who has come in off the long road; then the arena masters reining in the Hero Balloon's skeleton troopers and the Hero Goblins; then the weekly Monday reshuffle of what may be carried into the arena. **Written to the bar's rules:** Ronin is a *person*, not a card; "the arena masters", never a balance team; no percentages, no tile counts, no patch-note grammar. ⚑ **Supercell's event and album names ("Road to Ronin", the Wandering Warrior album) and the C.H.A.O.S acronym are deliberately kept OUT of the fiction** — they are product terms, not things a bar would say, and the fan-content line is safer without them. Only what *happened* is retold. Refreshing next season = rewrite this one file. No code, no schema, no redeploy.
- **`src/tome.js`.** `todayEdition()` resolves live-vs-evergreen and is prepended to the Herald tab as entry 0, **ungated** — a live companion always has today's paper, so the Herald tab is no longer blank on a fresh save. ⚑ **The date maths is the whole feature:** `validUntil` is compared as a **local calendar date**, never `new Date(iso)`, which parses `"YYYY-MM-DD"` as **UTC** and would retire the edition a full day early for every player **west** of Greenwich (in New York, local midnight on 3 Aug is already 3 Aug 05:00 UTC, which sorts *after* the parsed UTC midnight). Rolled-over dates (`2026-02-31`) are rejected too. The caption gained an optional `dateline`; the four story editions have none, so their caption is byte-identical. `story3` added.
- **⚠️ A REAL DEFECT MY RIG MISSED, CAUGHT BY LOOKING AT THE SCREENSHOT.** The first draft of the edition **overran the landscape page by 58px and the third paragraph was silently clipped** — the Tome has no scrollbars (GDD §11) and the Herald page does not paginate. My overflow assertion only ran in **portrait**, where it fit. Copy trimmed (and the dateline shortened so the caption is one line); landscape headroom now **70px**, roughly three spare lines, so a failed webfont load can't clip it either. The rig now asserts **no clipping for every edition in both orientations**, and `data/today.js` carries the word budget in a comment.
- **VERIFICATION — 0 failures.** `validUntil` inclusive at **23:59 on 3 Aug** and retired at **00:01 on 4 Aug**, proved in **six timezones** (Honolulu → Auckland) with a faked browser clock. ⚑ Note for future rigs: Playwright reads a naive `datetime` as the **test machine's** local time — my first pass put Los Angeles back on 3 Aug and looked like a code bug. Pin the zone. Every fallthrough lands on evergreen (malformed date, rolled-over date, missing `validUntil`, empty `live`, and `data/today.js` deleted entirely → the four back editions still render, console clean). The epilogue edition still prints its rattled consequence line and the decree-lifted closer. Runs from **`file://`**. **Desktop + portrait pixel-diff vs v34 across 11 states: the Tome's Herald tab is the only surface that changed; everything else 0px.** (Isolate `#fx` — a RAF canvas that `animation:none` cannot pause — and `#build-stamp`, which legitimately reads v34 vs v35.) Rigs: `scratchpad/r20_verify.py`, `r20_file_and_shot.py`, `r20_diff2.py`.
- **README updated** — R19's "the seam is cut, nothing is plugged into it yet" was true for about an hour. New *Today's Edition* section + `captures/readme/todays-edition.png`.

**ROUND 19 (10-07) — README GLOW-UP: the repo is a shop window now.** The live site's footer links this repo, so `README.md` was rebuilt as a landing page: hero banner, the pitch, three loud links, feature sections with real captures, one animation, an honest "how it was made", and the site's fan-content disclaimer verbatim. **No game code touched, so no version bump** — nothing the cache-bust token governs changed.
- **CAPTURES (new, `captures/readme/`, ~8.6 MB).** Every still driven through the game's own render functions and its own data files — no hand-typed copy, no mocked DOM. Captured at `device_scale_factor=2`, LANCZOS-downsampled. `hero-group-rally.png` (1400×700, the real Night-3 finale, HUD dropped and bubble suppressed — a banner wants the cast, not a half-filled bubble), `serve-and-brew.png` (the Wizard's **own brew gate**, pulled from `NIGHTS` at runtime: his `orderShort` in the Order Strip, his `mix` in the mixers, `The Showstopper ✓` matched), `tome-ledger-spread.png`, `arena-herald.png`, `night-cap-card.png`, `songbook.png`. Rigs: `scratchpad/r19_readme_stills.py`, `r19_fix_two.py`.
  - Two shots were re-taken **after I looked at them**: the Order Strip was rendering **empty** (a bare bar and an "i") because `Brew.unlock()` outside a real beat never sets an order; and the hero's group bubble showed only the first short chunk of the Knight's rally, plus the ▼ paging chevron.
- **THE GIF — `tome-page-turn.gif` (800×450, 47 frames, 160 colours, 4.02 s, 4.37 MB — under the 8 MB cap).** ⚑ **No `ffmpeg` in this environment**, so no `palettegen`/`paletteuse`. Substituted the same two-pass idea in Pillow: one **global adaptive palette** computed over a montage of every frame, then each frame mapped onto it with **Floyd–Steinberg dithering**. Measured 70 distinct colours across 300 px of flat parchment — no banding, not muddy.
  - **Frames captured deterministically, not by racing a wall clock.** `flipTo()` drives six CSS animations sharing one 0.62 s window with no delays, plus two `setTimeout`s (the DOM swap "beneath the covers" at 300 ms, cleanup at 640 ms). The rig stubs `setTimeout` to **catch** those two, pauses every `#pageflip` animation, walks `animation.currentTime` in exact 30 ms steps (30 ms = 3 centiseconds, GIF-legal), and fires the DOM swap at its real 300 ms mark. No jitter, no screenshot-latency drift. Rig: `scratchpad/r19_tome_gif.py`.
  - **Checked against a control** (a real, unmodified, real-time flip): the book genuinely goes **blank** mid-turn — `#pageflip`'s full-bleed backdrop is the empty `Tome Spread.webp`, which is exactly what "hidden beneath the covers" means. The GIF is faithful to the shipped animation, not broken. Signed-off surface; left alone.
- **⚑ THE HERALD CLAIM — flagged, not repeated.** The live case study says: *"There's a live 'Today's Edition' in the Herald reporting the current Clash Royale season as bar gossip, refreshed from a small file when the season changes, with an evergreen fallback."* **No such code exists** — verified across `data/`, `src/`, `strings/`, `index.html`, `cap/`, and `git log -S` over **every commit on every branch**. `HERALD_EDITIONS` is four static, story-bound editions (Ed. XLVII–L). The README therefore describes what the Herald *is* (an in-world paper; the Knight's last cup rewrites the morning headline; an edition is pure data, so a new one is a data change and not a code change) and says plainly that **the seam is cut and nothing is plugged into it yet**. The case-study copy is Tessa's call — it lives in `site/` (landing session's territory) and was not touched.
- **DEPLOY.** `DEPLOY-COMPRESSION.md` gained a **§4**: `captures/readme/` must be excluded from the player zip alongside the `*.md` dev docs. Nothing in the game loads it; it exists for GitHub. Also confirmed live, direct, no-redirect `200`s on `/play/`, `/case-study/`, `/cap/` and the root — **which closes the standing R16-5 flag: `/cap` is now served at the domain root.**

**ROUND 18 (10-07) — FRESH-PLAYER POLISH: music volume, toast dwell, first-brew mixer hint, build v34.** Tessa's developer played the game blind and hit three things; GDD §11 gained a "Fresh-player locks" block. Deck captures taken from this build.
- **1. DEFAULT MUSIC VOLUME 70 → 50.** `PREF_DEFAULTS.musicVol` in `src/save.js`, plus the `#vol-music` slider's static `value` attribute (cosmetic — Settings overwrites it from Prefs on render). ✅ **Answering the question you asked: `musicVol` and `sfxVol` are SEPARATE constants in the same literal, so SFX is untouched and stays at 70.** "Fresh saves only" comes for free: a default only ever fills a key the player has never set, so anyone who has touched the slider keeps their value. Verified: fresh → 50, sfx → 70, an existing `musicVol:90` survives a reload.
- **2. BREW-GATING TOAST — 5.6s → 3.5s, and it can no longer be held open.** `Screens.toast(msg, ms)` gained a duration (still 5600 by default, so the storage-unavailable toast is unchanged); `dialogue.js` passes 3500. **Anti-stack + anti-extend:** re-triggering the SAME message while it is still up is now ignored, so tapping the locked panel repeatedly can neither stack a second toast nor keep resetting the timer. Once it fades, a tap re-summons it. A *different* message still interrupts (a storage error must not queue behind a flavour line). Verified: up at 3.2s, gone by 3.9s **even after a re-tap at 1.8s**, exactly one `#toast` node, re-summons after the fade.
- **3. FIRST-BREW MIXER HINT.** A blind player couldn't tell the empty MIXER rows were tappable. On a fresh save, the first time the panel really unlocks, both mixer selects take **the same sage as Sage's choice buttons** (`--sage-pale`→`--cream` fill, `--sage-deep` border, sage ring) and a **3px bounce over 2.6s** — cozy, not a nag. **No arrows.** Never on BASE (pre-filled). Tapping a mixer stops the nudge (a bouncing row would drag its own dropdown around); **picking one retires it for good**, persisted in the save. Backing out of the picker without choosing leaves it armed for the next unlock. Save **v5 → v6** adds `hints:{mixer}`; a legacy v5 save infers it — *already poured* → retired (don't nag a returning player), *never poured* → armed once. **Play Again re-arms it**, because it lives in the save, not in prefs. Reduced motion keeps the highlight and drops the bounce.
  - ⚠️ **A bug I caught in my own fix:** the reduced-motion override was declared near the top of `main.css`, above `.slot.hint`. A media query carries **no extra specificity**, so it lost the cascade and the bounce still ran. Moved beside the rule it overrides, with a comment so nobody moves it back.
- **VERIFICATION.** 24 assertions × desktop 1280×720, portrait 390×844, and desktop under `prefers-reduced-motion` → 0 failures. Plus a **fresh save walked through the real first brew via the dialogue gate** (not by calling `Brew.unlock()`): hint appears at the gate → picker → pick Heal → retired → poured *The Usual ✓* → **Hog Rider's brew beat brings no hint back**. Legacy-save inference tested both ways. Desktop pixel-diffed against R17 across the same 10 states: **0 differing pixels**. Console clean in Chromium, Firefox and WebKit, both orientations. Rigs: `scratchpad/r18_verify.py`, `r18_walk.py`, `r18_deck.py`.
- **4. DECK CAPTURES** (in `captures/r17/`, portrait 390×844 @3× = 1170×2532, no browser chrome; every line of dialogue pulled from `data/nights.js` at runtime, never hand-typed): `deck-pekka-portrait-390x844.png` (her real opening line, bubble over her head, brew panel in its **closed/locked** state — what a player actually sees), `deck-pekka-portrait-390x844-nopanel.png` (same frame with the panel removed — ⚑ **staged**: the serve screen has no panel-hidden state, `.locked` only dims), `deck-duo-portrait-390x844.png` (the real Night-1 Hog+Knight two-shot), `deck-group-portrait-390x844.png` (the real Night-3 finale, all five in frame). The last two are the letterboxed `contain` framing for Tessa's sign-off.

**ROUND 17 (10-07) — MOBILE PORTRAIT: character framing, bubble anchoring, panel fill, build v33.** After Tessa played on her real phone (iPhone, portrait) and the staging had drifted from the desktop rules. GDD §11 gained a "Mobile portrait staging" lock block; all five items closed. **Desktop is pixel-identical — proved, not assumed.**
- **ROOT CAUSE (one line, and it explains items 1 and 2).** The portrait scene band is 720×819 and the art is 2048×1143 (1.79:1), so `object-fit:cover` throws away **51% of the width**. The CSS said `object-position:center top`. The whole cast is hand-grounded at head cx ≈ 0.21, so `center` put every character's head at **x = −64px** — the Knight half off the left edge, exactly Tessa's screenshot. The bubble then anchored to a head that wasn't there, tail pointing at empty counter.
- **NEW `data/framing.js` — measured, not eyeballed.** Every serve scene is 2048×1143 and (per the round-9 canon) shares a **pixel-identical background** with `Empty Bar (no characters)`, so each character's bounding box was derived by **differencing their scene against that empty plate** (`scratchpad/r17_measure_scenes.py`). Result across all 20 scenes: head centres span only **0.206–0.214**, widest body (the Knight) **0.050–0.379**, head-tops 0.233–0.290. That confirms the cast really is on one anchor.
- **1. CHARACTER FRAMING.** `Screens.soloFocus()` computes the portrait `object-position` that lands the head at **targetX 0.40** of the band, then **clamps it so the widest body can never leave the frame**. It resolves to `2.796% 0%`. **One value for every solo scene, including the empty bar — and it has to be:** the backgrounds are pixel-identical and cross-fade on an expression swap, so a per-scene focal point would make the whole bar jitter every time a face changes. Landscape clears the inline value entirely, so the signed-off desktop framing is the CSS default it always was. **Audited all 5 characters × all 4 expressions (20 scenes) × 390×844 and 360×800: every body fully in frame (min left margin 52.9px, max right 535.9 of 720), every head at 39–41%, one object-position, zero jitter.**
- **2. SPEECH BUBBLE.** Two bugs. (a) The head-top was `c.head × stage height`; in portrait the scene band is only 64% tall, so the bubble dropped **~108px onto the speaker's face**. It's now `c.head × the scene band's height` — which in landscape *is* the stage height, so desktop maths is byte-identical. (b) In portrait the bubble was pinned at `left:4%` with a fixed 52% tail. It now centres on the reframed head (per-expression `cx`) and sets `--tailx` in px; `#serve-bubble:after` takes `var(--tailx, 52%)`, so landscape keeps its fixed tail. **Tail lands within 0.6px of the measured head on all 20 scenes.**
- **3. SAGE AT THE COUNTER — already correct; verified and made robust.** The before-captures show her bubble and her three options already sitting at the bottom of the scene, on the counter. What was wrong was the *customer's* bubble floating above them. The anchor is now tied to the **scene band's bottom edge** (36.8%) rather than to the panel's top, so it can't drift if the panel moves. ⚑ **Deliberately NOT widened**: they leave an 18% gutter on the right, but that was not broken and Tessa didn't ask. Say the word and they fill the width.
- **4 + 5. BREW PANEL — the wrapped flex row was the shared cause.** `.panel-cols` was `flex-wrap:wrap` with `slotcol:44% + rightcol:48%` (leaving a **37px dead strip on the right**), and the wrapped line **stretched**, so the columns grew a slack pocket under the mixer selects while the meter block was pushed down onto the slider. Replaced in portrait with a **grid**: `grid-template-columns:1fr 1fr` (columns now fill the box — dead-right **37px → 0px**) and `grid-template-rows:auto 1fr` with `.meters{align-self:center}` (row 1 hugs the selects; the meter block centres in the space that's left). **Measured: gap above 28.9px, below 25.9px — 3px off dead-centre.**
- **1 (multi-character). DUO + GROUP.** A 1.79:1 frame cover-cropped into the 0.88:1 portrait band keeps ~49% of the width — not enough for two characters, and nowhere near enough for five. The finale was **cutting Hog Rider and Princess out entirely**. Both now use `object-fit:contain`, the only framing that keeps every character in frame per the GDD lock, with the name-box brought up to meet the scene and the letterbox dressed in the screen's own dark wood. ⚑ **Trade-off, flagged:** contained portrait art is inherently smaller and leaves a dark lower half. The alternatives are bespoke portrait duo/group art, or showing one speaker at a time. **Tessa's call — the lock said "fully in frame", so that is what shipped.**
- **VERIFICATION.** Full Night-1 open walked at **390×844 and 360×800**: Knight arrival → bubble on his head → 3 choices at the counter → brew panel opens with the Order Strip → mixed **The Usual ✓** → poured. **Desktop 1280×720 pixel-diffed against the pre-round build across 10 states** (5 characters, Sage's bubble, the choice stack, group, group-narration, duo): **0 differing pixels** (the first pass showed 7–23 px, all of it the animated ▼ paging chevron caught mid-animation — pseudo-elements aren't matched by `*{animation:none}`). Rotation landscape↔portrait↔landscape returns the bubble to its exact original coordinates. Console clean. Rigs kept in the scratchpad: `r17_measure_scenes.py` (bbox by differencing), `r17_audit.py` (20 scenes × 2 viewports), `r17_pour.py` (walk + panel asserts), `r17_desktop_diff.py` (worktree of HEAD served on :8642, pixel-diff).
- **Adversarial review — no high/medium defects; 3 LOW, all closed.** It confirmed by arithmetic that the clamp is not inverted at the shipping band; that a hidden serve screen degrades to `left top`, which still frames the character (that is exactly why the CSS fallback was changed from `center top`); that `#order-strip` is a **sibling** of `.panel-cols`, so the grid cannot touch it; that the picker still escapes upward unclipped; that `translateX(52%)` still lands the right duo character correctly under `contain`; and that no `lastServe.headTopY` reader survives. Fixed anyway: **(1)** `soloFocus()` had no guard for an inverted clamp — unreachable at the locked stage (it needs a band aspect > 1.628 vs our 1.137) but it now centres the body rather than returning nonsense; **(2)** a zero-size scene layer (hidden screen) and a zero-width bubble now fall back to the design band / CSS width instead of un-framing the character; **(3) a real regression I had introduced** — `#duo-split`'s diagonal seam and its "silent half dims" gradient still spanned the whole screen after I shrank the duo band to 36%, washing over the letterbox and the name-box. Now scoped to the scene band. Meters re-asserted with the Order Strip live at four portrait sizes: no overlap with the slots, the dose or the panel edge.

**ROUND 16 (08-07) — final tidy: share-page Sage, Tome lanes, download taint, sage Share, build v29.** After Tessa's round-15 review. All five items closed; build still launch-zip ready.
- **SHARE-PAGE SAGE — transparent cutout, big, waist-cropped, note beside her head with a hand-drawn line.** The page now loads `assets/characters/sage-nobg.webp` (550×1200 RGBA, ~102 KB — Cowork's transparent web asset, **committed**), sat directly on the indigo gradient with no box or backdrop. She's scaled up (170px → `clamp(250px, 27vw, 330px)`) and **cropped in CSS at the waist**, just below the tankard — `aspect-ratio:550/806` + `overflow:hidden` keeps the top 67.2% of the full figure, so the crop is a framing decision, not a new asset. Her aside moved from *above* her to *beside her head*, in the clear lane left of her silhouette, and a **hand-drawn green line** now runs from the handwriting to the sentence "Decide how your customers' nights end." (wrapped in `#cap-target` so it has a rect to aim at). The line is `placeArrow()` lifted verbatim from `src/tome.js` — same three-curve table, same rigid translate/rotate/scale, same `non-scaling-stroke` — with two deliberate differences: no stage-scale division (this page has no scaled stage), and the stroke takes the aside's ink `#a9c58a` instead of the Tome's `--sage-deep` `#6B7F5C`, which is tuned for cream paper and vanishes on indigo. **"Clear of the other text" is enforced in code, not eyeballed:** the connector tries every line box of the target and several anchors on each, rejects any segment crossing another *glyph* rect (line boxes, not element boxes), and keeps the shortest survivor. Verified at 1280 / 390 / 360: line present, crosses nothing, ≥15.8 px from other type, exactly 10 px off the sentence, no horizontal scroll. On mobile she bleeds flush to the left edge so the aside gets the right corridor — the only one that clears the centred Play button. Copy unchanged.
- **TOME PAGE LAYOUT — the body text now uses the page; the notes got their own lane.** A **spacing pass**, exactly as asked: R15's headless lines are untouched. `#spread` was widened onto parchment it was leaving unused (`18.5%/62.5%` → `15.844%/67.813%`, **+34 px each side, symmetric about the painted spine** so the gutter stays on the crease; the left limit is the lit-paper onset at ~15.5%, not the paper edge, so type never rides onto the curved page edge). `#tomenav` **deliberately keeps the old rect** — its nav percentages were measured against it. The lanes then moved **out of the text column**: `#spreadflow` takes JS-set left/right margins (the outer lanes), the inner lanes live in the gutter, and `.ledger-bio` padding drops **74 px → 2 px**. `laneMetrics()` derives the whole budget — `LANE = NOTEW + STROKE + CLEAR + EDGE` — so **by construction** a note can never come closer than `CLEAR` to the type and the connector always has a real run to draw. Connectors now **stop 13 px short of the printed text** (the phrase's own underline identifies it, §R15) and are **clamped into their own note's vertical band**, which is what stops a collision-stacked note dragging its line up across the intervening lines. **Result, measured on all five spreads at 1280×720:** body text **176 px → 236 px per page (+34%)**; note→text clearance **19–21 px → 34–39 px**; connector→text clearance **0 px (several literally overlapping) → 14.5–19.1 px**; **16/16 rule violations → 0**. Portrait: text **120 px → 161 px (+34%)**, notes held at 47 px by an 80 px lane floor (a bare proportional lane collapsed them to 38 px). Re-shot all five spreads at 2×; page-turn re-verified on the paginated Princess spread (translate `-888` = 2×(colW+gap), nav flips, returns clean). One knock-on, flagged: the narrower column made "Hog Rider" wrap, so the head row was tightened (`gap` 18→14 px, nameplate 29→26 px, **scoped to `#spreadflow`**) — every name back on one line.
- **NIGHT CAP DOWNLOAD — diagnosed, and it is NOT what it looked like.** Measured, not assumed, across Chromium / WebKit / Firefox × `http` / `file`: **over http(s) the canvas was never tainted and Download already worked** — in-game and on `/cap`, desktop and mobile (a real 2,066,225-byte PNG). The failure is **`file://` only**: Chromium and WebKit taint the canvas *no matter what*, so `toBlob` throws `SecurityError`, and the old empty `catch` swallowed it — a dead-looking button. Firefox neither taints nor objects. ⚠️ **The brief's fix (b) would have made it worse:** setting `crossOrigin='anonymous'` on a `file://` image makes Chromium and WebKit **refuse to load it at all** (verified) — trading a broken download for a blank card in two engines out of three, on a build whose CLAUDE.md requires it to run from disk. So `needsCORS()` sets `crossOrigin` **only for a genuinely cross-origin http(s) asset** (none today; it future-proofs a CDN move) and never on `file:`. What actually changed: (a) nothing is swallowed — a tainted export now raises a two-line toast in-game ("Couldn't save the card." / "Saving only works from the website, not a file on your computer.") and an inline message on `/cap`; (b) `toBlob()` catches the **synchronous** throw and never resolves a null/empty blob (that used to make a failed export look like a successful no-op); (c) `isTaintError()` separates the taint case from a generic retry. Whether the browser shows a Save-As dialog or drops the file into Downloads is the viewer's own setting — not page-controllable. **Verified 13 ways:** 3 engines × 2 origins × {in-game, /cap} + mobile; card renders in every cell; the taint toast confirmed live in Chromium and WebKit. ⚑ **Net: the deployed site was never broken.** Tessa hit this opening the build from disk.
- **NIGHT CAP BUTTONS — Share is sage, Close stays cream.** One class: `#nightcap-share` → `btn small sage`. No CSS needed — `.btn.sage` already existed and both buttons that *open* the Night Cap (`btn-nightcap-end`, `btn-nightcap-epi`) were already `btn small sage`, so Share now matches the thing it belongs to. Verified computed: Download purple `#B26AE0→#7A3FA0`, Share sage `#C7D3B5→#9CAF88`, Close cream `#fdf6e8→#F4ECDD`.
- **/cap — CONFIRMED, no code change.** Served at domain root: `/cap/` resolves, `<base href="../">` resolves to root, **every subresource returns 200 on a root-relative path** (`/css`, `/src`, `/data`, `/assets`, `/strings`), the card rebuilds from the fragment, and "Play Elixir Hour" → `/index.html`. It redirects to the landing page today only because `/cap` isn't served on the live domain yet. Cowork includes `/cap` in the launch zip; the developer deploys at root. ⚑ **Standing deploy flag:** if the game ever deploys under a subpath, the `<base>` and `shareLink()`'s `GAME_URL` both need adjusting.
- ⚑ **New player-facing copy, for Tessa's approval (English only; the other six fall back via `t()`, per the canon-term convention):** `nightcap.download.failed` = "Couldn't save the card."; `nightcap.download.taint` = "Saving only works from the website, not a file on your computer."; `nightcap.download.retry` = "Something went wrong — give it another go." Also `nightcap.share.copyfail` from R15.
- **Adversarial review (26-agent, review→verify) — 13 confirmed, ALL fixed pre-commit; 9 refuted.** It caught **two HIGHs I would have shipped**, both because I'd verified only the viewports I'd designed against:
  - **(HIGH) Portrait cropped every Ledger card.** The narrower column (161 px) plus a `.spread-head` with no side padding flex-shrank `.bigclip` — which has `overflow:hidden` and an absolutely-positioned `<img>`, so its flex `min-width` resolves to 0 — shearing the art rather than wrapping the name. **P.E.K.K.A lost 58% of her card** (110 px → 44.1 px). A true R16 regression: pre-R16 the same row overflowed by only 2.8 px. Fixed with `.bigclip{flex:0 0 auto}` plus, in portrait only, the name dropping under the card (no card+name row fits 161 px at all). Landscape keeps its side-by-side row with 8 px of slack (nameplate 26→25 px, gap 14→12 px). Verified: no shear, every name one line, both orientations.
  - **(HIGH) The share page was broken from 721 px to ~1131 px.** The two-way split needs ~1180 px (card 340 + gap 36 + pitch 340 + Sage 330 + her aside's 0.42×width overhang); below that Sage painted over the pitch copy — covering the Play CTA at 721–831 px — and the aside sat on the type at 1024 px, the exact thing its own comment forbade. Added a **compact-desktop tier (920–1219 px)** where card, pitch and Sage all give ground together, and **raised the stack breakpoint 720 → 919 px**. Now swept at **27 widths, 320→1920: 0 failures** (line drawn, crossing no glyph and no button pill, Sage and aside clear of the type, no horizontal scroll).
  - Also fixed: `loadImg()` cached **rejected** promises, so one transient image failure permanently killed the card for the session (now evicts on error); `open()` awaited `compose()` with **nobody catching** the rejection — a silent blank card plus a live Download exporting the half-drawn canvas (now try/caught, and Download/Share are **disabled until compose resolves**, verified: a mid-render click no longer downloads); the same on `/cap` (button ships `disabled`, enabled on render); the connector's collision test saw only **glyph** rects, so it could be drawn across the Play CTA's **painted pill** (buttons now block as whole element boxes) and it **silently drew nothing** at 721–741 / 961–991 / 1061–1111 px (the pad now relaxes 5 px → 2 px before giving up, and never to 0); `#cap-dl-err` reflowed the wrap when shown, desyncing the already-drawn line (now absolutely positioned); `#nc-toast` was permanently `aria-hidden` despite being the *only* feedback a failed download gives (now `role="status" aria-live="polite"`).
  - **Refuted (9):** the `aspect-ratio` fallback nit, an rAF-thrash claim (`draw()` is idempotent), the curved-bezier-vs-straight-chord claim (unreachable magnitudes), an opaque-origin `location.origin === "null"` claim (empirically false in all three engines), and five smaller comment/a11y nits.
- ⚑ **For Cowork / the launch zip:** `assets/characters/Sage Final.png` (**3.98 MB**) is now referenced by nothing — the share page uses `sage-nobg.webp`. It's still tracked, so it ships in the bundle. **Not deleted** (art files are Tessa's call); flagging it as free weight to drop from the deploy zip.

**ROUND 15 (08-07) — polish: headless arrows, Night Cap share, scrollbar, epilogue paper, build v28.** After Tessa's Night-3 wrong-drink playthrough.
- **TOME ARROWS — arrowheads DROPPED entirely (Tessa's final call).** After four rounds of head/tail bugs, the head is gone: each annotation is now ONE hand-drawn green line (a gentle organic curve, same 1.8px weight) from the note to its phrase. The printed phrase stays **underlined** — that marks it, so there's no direction to be inconsistent about and nothing left to misalign. Endpoints keep the R14 note-anchored + clamped placement (6-10px clear of text). Re-shot all five spreads at 2× — clean lines throughout.
- **NIGHT CAP — Download + Share wired, and the share page built (§R15).** **Download** = the card PNG only → `elixir-hour-night-cap.png` (the personalised card, NOT the og:image; `night-cap-share.png` untouched). **Share** = a self-contained LINK on the game's domain: `https://elixirhour.tessa-kerk.com/cap#<base64url>` with the run summary **encoded IN the fragment** (`{n, dt, v:[LEDGER idx], k:[BREWBOOK idx]}` — title + quote derive from the night, so the whole card rebuilds from a ~63-char fragment; **no server/DB/expiry**). Mobile → native share sheet (caption + link); desktop → copy + a **two-line toast** ("Link copied." / Sage's green "Send it to someone who needs to head to the bar!"). **The share page** (`game/cap/index.html` + `cap.js`) reuses the game's OWN renderer (`NightCap.renderTo`) + data + assets (no duplication, `<base href="../">`), decodes the fragment and rebuilds the SAME card; landing-page palette (indigo bg, purple Play, Baloo 2 / Cormorant), two-way split (card left, pitch right), Sage cutout in the corner with her green aside. All approved copy verbatim. Verified: encode↔decode round-trip, desktop copy+toast, share page desktop + mobile (card rebuilt from the link). ⚑ **DEPLOY FLAG (Cowork/dev):** the share page assumes the game is served at the DOMAIN ROOT (so `/cap/` = `game/cap/` and the `../` base resolves to `/assets`, `/src`, `/data`, `/css`). If the game deploys under a subpath, the `<base>` + `shareLink` root need adjusting. One added route: `/cap → game/cap/`.
- **BREW BOOK + SONGBOOK SCROLL.** The recipe/track lists (in `#pageL`) outgrew the page (`overflow:hidden` cut the bottom off) — now `overflow-y:auto` with a **slim auto-hiding scrollbar** (light-brown, no rail, fades in while scrolling via a `.scrolling` class, out ~700ms after). Verified: 7 recipes overflow + scroll; the Songbook shares `#pageL` so it's covered. (Page-flip version deliberately NOT built, per Tessa.)
- **WRONG-DRINK EPILOGUE — on the paper PROP, not a CSS card.** The epilogue Ed. L text sat on a plain CSS card (`#epi-sheet`) while every other full-screen Herald prints on the generated 3D Arena Herald paper — breaking the 3D look on the wrong-pour path. `#epi-sheet` now uses the SAME paper prop + framing as `#broadsheet` (embossed masthead baked in, article in the flat parchment; type a touch smaller to fit the extra copy). Verified side-by-side: wrong-pour epilogue vs a Night-1 Herald, same paper, same framing, nothing clips. (The counter-wood backdrop from R13 stays.)
- **Adversarial review (14-agent, review→verify) — 6 confirmed, all fixed pre-commit; 5 refuted.** All six were robustness holes in the NEW share code (the externally-facing part — people open these links), none regressions: (1 MED) mobile `navigator.share` rejection was swallowed by an empty catch with no fallback → now distinguishes user-cancel (`AbortError`, no-op) from a genuine failure (embedded without web-share, etc.) and falls back to copy-then-toast so the link is always reachable. (2 LOW) the copy path reported "Link copied." unconditionally (clipboard rejection hit the *success* toast; `execCommand` false still resolved) → `copyLink` now rejects on `execCommand`-false, and failure surfaces the link via `window.prompt` (new `nightcap.share.copyfail` string) instead of a false toast. (3 LOW) `decodeState` had no cap on the decoded index arrays → a crafted `/cap#` fragment with huge arrays could freeze the opener's tab (reflected DoS); now `slice`d to `LEDGER.length`/`BREWBOOK.length` (verified: 50 000-element arrays → capped to 5/7 in 3 ms). (4 MED) the share page's `showErr` hid only `.cap-card`, leaving a dangling "Download card" button on every error path (could save a blank/partial PNG) → now hides the whole `.cap-left` column. (5 LOW) the share page's `og:image` was relative with no `og:url` — crawlers ignore `<base href>` and would 404 the preview image → absolute `og:image`/`og:url` + `twitter:` tags added. (6 LOW) `fmtISO` printed the literal "Invalid Date" for a malformed `dt` (`toLocaleDateString` on an Invalid Date returns a string, doesn't throw) → `isNaN` guard blanks it. The 5 refuted: positional-index encoding "needs a version field" (data is locked, over-engineering), epilogue paper clips at phone/desktop sizes (false — the stage is a fixed 1280×720/720×1280 design space, not fluid), and three narrower variants. Re-verified all six live (share page success + error paths, desktop toast success + honest-failure prompt, DoS cap, blank-date).

**ROUND 14 (08-07) — FINAL POLISH: arrow layout, two-line Continue, build v27.** After Tessa's full playthrough (incl. the wrong-drink path). Build ready for the launch zip (Cowork builds it).
- **TOME ARROWS — LAYOUT fix (the glyphs were right; the placement wasn't).** Per the Ledger Page Mockup (text at ~46% width with deep margins), the bio side padding widened **58 → 74px** so the notes sit in a real margin, not crammed against the text. Notes now **hug the edge facing their phrase** (`text-align:right` in the left margin, left in the right) so the **tail starts AT the handwriting**, never in the ragged whitespace. The **§0 direction rule is enforced**: EVERY arrow flows note→printed-phrase — the tail is anchored at the note's inner edge and a clamp moves the TIP (never drags the tail across the note), so a line-start phrase can no longer flip an arrow backward. **Audited + re-shot all five spreads at 2×** (16 arrows) — all consistent, tails at notes, heads at phrases, nothing clips. ⚑ **Flag:** the wider margin narrowed the bio text column (a signed-off surface — reflows to a slightly narrower measure, which reads well + matches the mockup). Approved-surface change, in service of the requested arrow fix.
- **TWO-LINE CONTINUE (every night).** The Continue button now matches Play Again: "Continue" (Baloo 2) over a smaller "Night N: Title" (Nunito) — no orphaned words in any language. Verified: `two-line` class + `.btn-main`/`.btn-sub` for a Night-2 save.
- **WRONG-OUTCOME EPILOGUE — already resolved in R13, verified both variants.** R13's `#epi-wrap` wood-backdrop change applies to the WHOLE epilogue screen; both consequence variants (clear + rattled) render on the counter-wood — they differ only in the epi-conseq text. Confirmed live (CSS) + rig-captured the rattled Ed. L on the wood. No code change (Tessa likely saw a pre-R13 build).
- **DECISION — music persists across Play Again (INTENTIONAL, Tessa's ruling, do NOT "fix"):** restarting from Night 1 (Play Again / New Game) KEEPS the current Songbook track rather than resetting to *Sage's Favourite*. `Game.newGame()` resets the save/state but not `Prefs.track`, so `Sound.refresh()` keeps the player's chosen tune. This respects the player's choice and avoids an immersion-breaking reset. **Any future session: leave this as-is.**
- **Adversarial review (multi-agent) — 1 confirmed, fixed pre-commit:** changing the **language on the title with a save present** left the two-line Continue button stale — `fillStatic()` reset it to a plain "Continue" label but kept the `two-line` class/flex layout (no sub-line). Fixed by re-rendering the title in `settings.js applyLanguage` when it's the active screen (mirrors the herald/nightend re-renders). Verified: de → "Fortsetzen / Nacht 2: The Draw" (two-line intact); no-save → single-line "Continuer", disabled.

**ROUND 13 (08-07) — polish: glyph arrows, dip transitions, WebP twins, build v26.** GDD §11 Round-10 locks (+4) + §13 audit closed. All landed + verified live (the preview viewport worked this session — arrows shot at native 2× across all five spreads).
- **TOME ARROWS → single-path glyphs (third strike, the tailspin rule).** Retired the procedural head+tail entirely. An arrow is now ONE pre-drawn single-path SVG glyph (shaft + head one continuous stroke, so they physically cannot disagree) — a 3-glyph library (`ARROW_GLYPHS` straight / curve-up / curve-down, hand-drawn, green), placed by a rigid translate+rotate+uniform-scale from the note toward the target, `non-scaling-stroke` keeping the 1.8px line constant. The belly dips into the inter-line gap; both ends 6-8px clear of text. **Verified at native 2× on ALL FIVE spreads** (16 arrows: Knight 4, Wizard/Princess/P.E.K.K.A/Hog 3 each) — every head clean, aligned to its shaft, clear of words. Removed the R12 `drawArrow` + its `.arrowhead` CSS.
- **EPILOGUE HERALD backdrop.** `#epi-wrap` was a dark radial; now it shares the **counter-wood Herald backdrop** like every other full-screen interstitial (the one missed surface). Verified: the parchment edition + cream closer read cleanly on the wood.
- **LAYOUT-CHANGE DIP.** Switching between two DIFFERENT in-bar layouts (serve↔duo↔group) now uses a **warm near-dark camera cut** — `#dip` fades in ~200ms, the layout swaps UNDER the cover (screen switch + beat start, so the bubble still paginates), fades out ~250ms — instead of cross-fading two layouts into a double-exposure. Same-layout swaps (expression / character enter-leave) keep the cross-fade, untouched. Reduced-motion skips the dip. `state.js` `layoutShow()` gates on `current`≠target with both in {serve,duo,group}. Verified: dip fires + clears cleanly, screenfade suppressed under it.
- **TITLE SETTINGS KEEP THE DUCK.** Reverted R12's Settings-open un-duck (Tessa's call): opening Settings on the title no longer jumps the music to full — the duck stays a multiplier on the slider level until play starts; the slider still works (its % changes the underlying level, heard at the ducked level). Removed `settingsOpen`/`setSettingsOpen`. Verified: `ducked` stays true through Settings open/close on the title.
- **WEBP TWINS SWAPPED (Cowork baked them).** All 15 shipped UI PNG refs → their `.webp` twins (~35 MB → ~4 MB): Tome Spread, 5 cards (q92, lossless-overshoot — gilt zoom-QA'd), 3 brew panels, phonograph, tankard, logo, nav-arrow, card-select, night-cap-scroll. `night-cap-share.png` stays PNG (og/twitter). Verified: cards + Tome Spread + brew panels render identically (spread + serve captures). The ~36 MB source PNGs stay in place — Cowork excludes them at the Tools level for the launch zip.
- **TRANSLATIONS audit CLOSED** (§13, Tessa→Cowork ruling): the 6 unverified canon terms stay English per the locked no-guess rule; the in-product disclaimer covers it; patchable post-launch. Audit table marked resolved.
- **Captures:** all five Ledger spreads + the epilogue re-shot (native 2×); the four marketing captures re-shot on v26.
- **Adversarial review (multi-agent) ran on the diff — 5 confirmed, all fixed pre-commit:** (1 HIGH) the dip removed `dipping` in a delayed timer → the revealed screen re-fired its screenfade → a flicker on EVERY layout change; fixed by dropping `dipping` synchronously with the swap so the new screen's ONE screenfade is the reveal (under the fading cover). (2 MED) overlapping dips from fast advancing could race a stale cleanup; fixed with a cancellable timer handle. (3 MED) the glyph head scaled with arrow length (short arrows → blob heads, a regression from R12's fixed head); fixed by building each head at a constant ~9px on-screen size from the shaft's end tangent (still one continuous path). (4 LOW) the straight-guard was one-sided (steep UP arrows C-bowed); made it `|dy|>18`. (5 LOW) stale audio.js header comment. Re-verified: dip removes `dipping` synchronously; arrowheads now uniform + legible on the shortest arrows (Knight + P.E.K.K.A re-shot).

**ROUND 12 (08-07) — volume sliders + audio polish + Tome fixes, build v25.** GDD §11 Round-10 locks (+4 entries) + Settings Sound now sliders. All landed + verified (live eval + rig captures — the preview tab's viewport was stuck at 0 this session, so visual QA ran through the capture rig at native 1280×720).
- **VOLUME SLIDERS (§11).** Settings' Music/SFX on-off toggles became **two live 0-100% sliders + a speaker-mute each** (dose-slider accent). Slider %×ceiling → gain, so the default **70% lands on the approved levels** (music 0.504, sfx 0.595); ducked-on-title math preserved (0.14). Slider-at-0 = muted; the mute toggle remembers + restores the last level. Volume applies **live while dragging**; Settings-open lifts the title duck so the slider is audible. Values + mute states persist in Prefs (verified: 45/55 survive reload). New Prefs schema `{musicVol,sfxVol,musicMuted,sfxMuted}` **migrates old `music/sfx` booleans**. Plus a **one-tap music-mute** (speaker) in the in-game menu tray, synced to Settings.
- **MUSIC STARTS ON FIRST GESTURE.** The first pointer/key interaction unlocks + starts the music (was: Tessa toggled a while before hearing anything — autoplay gate). If a track is still buffering, a tiny **"Music loading…"** note shows instead of silent nothing (confirmed rendering in the rig shot).
- **WEB-READY AUDIO + SFX.** Cowork's re-encoded 112 kbps tracks (~13 MB, was 21) swapped over the raw takes; the four CC0 SFX (`mug-clink`/`brew-fizz`/`page-turn`/`quill`, all <22 KB) dropped in — cues fire, no missing-file notes. Crossfade loop unchanged.
- **TOME OPEN-TARGET.** Opening the Tome now shows **whatever landed since it was last open** — a newly met regular → the **Ledger grid** (their card to tap), a new recipe → **that Brew Book page**; nothing new → restores the last position. Fixes the "closed on Knight, met Wizard, reopened to the stale Knight spread" report. (Generalised the old first-recipe/Knight-only onboarding hooks to fire on every unlock; character wins if both land together.)
- **TOME MARGIN ARROWS.** Rewrote `drawArrow`: **filled-triangle head at the path END, rotated to the end tangent, tail into the middle of the head's base** (never cutting its side); both ends pulled **5-7px clear of text**. Verified at native scale on the Knight spread — both left- and right-pointing arrows read correctly (the reported one-side-wrong case is gone).
- **ARENA HERALD PLACEMENT.** Interstitial: body text **re-centred on the paper's flat area** against the (asymmetric) masthead — nudged the padding so headline+story sit at ~646/640 vs paper centre 641 (measured). Tome Herald **tab reverted to text-only** (the 3D paper is reserved for the full-screen interstitial; text-only scales to any article length).
- **CREDITS.** About panel adds **"The four bar tracks are AI-generated (Suno)."** beside the AI-translation note, in all 7 languages.
- **Cross-session:** posted the exact UI-PNG WebP-twin list to "Cross-session requests" for Cowork; ~36 MB of superseded character source PNGs flagged delete-last (Cowork excludes at Tools level).

**ROUND 11 (08-07) — audio wired + duo fix + polish, build v24.** GDD §11 Round-10 locks (3 new entries) + §16 music resolved. All landed + verified live; compression + launch zip flagged (see `DEPLOY-COMPRESSION.md`).
- **MUSIC — Songbook wired (§16).** The four takes copied into `assets/audio/` and mapped to `SONGBOOK[].file`. `Sound` rebuilt around **two stacked `<audio>` layers** so each track loops via a **~2 s end-to-start crossfade** (the natural end-fade of one pass overlaps the head of the next — no gap, no restart pop; proven live: seam → both layers play → volumes cross → old layer pauses/resets → `active` swaps). The loop never seeks a tail byte (layer B starts from the cached head), so it's Range-independent. Music **ducks to 0.14 over ~1 s** on the title + rest screens and **lifts back to 0.5** on play (via `Sound.onScreen` in the screen switcher) — verified 0.14↔0.5. Unlock schedule (`songUnlocked`, unchanged) verified live: Night 1 shows only *Sage's Favourite*; *Rain*, *Little One's*, *Last Call* render as unspoiled `—/—` locked rows until earned. Settings music toggle pauses/resumes both layers. Track-switch from the Songbook loads + plays all four. `preload="metadata"` (not `auto`) so the game streams on play instead of pulling 21 MB up front.
- **SFX cues wired.** mug-clink (drink lands), brew-fizz (pour), page-turn (Tome nav), quill (autosave flourish, on `HUD.saved`). Files still to source — see `assets/audio/sfx/SFX-NEEDED.md`; a **missing file degrades silently** (one info note, deduped so no pour-spam, never throws — verified).
- **DUO overlap bug FIXED + re-shot.** The N3 two-shots now build on the **Empty Bar** base + both cutouts, with every cutout layer reset on `renderDuo` so no previous character leaks. Verified live (P.E.K.K.A + Hog): backdrop = Empty Bar scene, exactly two cutouts (z3 left, z5 right), both standby layers empty at desktop **and** portrait — no stray Knight. `captures/split-duologue.png` re-shot.
- **Herald interstitial** now renders on the **counter-top wood backdrop** (`Herald Backdrop (counter top-down).webp`) → brown paper → article; dark gradient retired.
- **Sage's-choice hover** fills **dark sage** (`--sage-deep`) with light text (was cream). **Mixer** dose-live label lifted clear of the slot (−10 top, gap 5). **Title Play-Again** button is two lines ("Play Again" / "from Night 1") — no orphaned words.
- **Compression sweep:** round-11's only new raster (Herald backdrop) is WebP (124 KB). **Audio compression BLOCKED — no `ffmpeg`/`lame`/`opusenc` in this environment**; tracks ship raw ~180 kbps (target ~112 kbps MP3 ≈ 10 MB). Loaded UI PNGs (~35 MB, Tome/cards/brew — approved+LOCKED surfaces) and ~36 MB of orphan source PNGs (`assets/characters/`, superseded by webp) are flagged for a sign-off pass + deploy exclusion in `DEPLOY-COMPRESSION.md`. **Launch zip pending that pass.**

**ROUND 10 (08-07) — UI locks + fixes, build v23.** GDD §11 "Round-10 UI locks" block. All landed + verified live except the flagged content gap.
- **Cross-fade dip-to-empty FIXED** (my v22 bug, `Playtests/Character Fades.mp4`): I was fading the outgoing layer DOWN while the incoming ramped up → two semi-transparent layers over dark = a visible dip. Now the outgoing holds fully opaque and the incoming ramps up ON TOP (z-index tiers), ~250ms ease-in-out. Solo + duo. Verified mid-transition: outgoing opacity 1 throughout.
- **New art (WebP):** (a) **Empty Bar** backdrop for every no-customer beat (`setServeCustomer(null)` → Empty Bar) — kills the blank screen behind Sage's opening. (b) **Bar Closed** rest screen (shuttered bar + CLOSED sign, centred scrim keeps the title/buttons legible) replaces the dark gradient. (c) **Arena Herald Paper (brown)** — the Herald tab renders the embossed masthead + article on the parchment.
- **Completed-save title state:** after the epilogue, Continue → **"Play Again — from Night 1"** (confirm before wiping) + a new **"Open the Tome"** button (hydrates unlocks, opens the finished keepsake over the title). `Game.isCompleted()` = the Night-4 epilogue Herald is archived.
- **Button colours:** title Continue/Play Again → sage `#9CAF88`; Settings cream; Start purple. Sage's 3-option choice buttons → sage fill (her colour) + dark-sage outline + hover lift (were cream).
- **Mixer:** slot column compacted so the bottom MIXER slot lifts clear of the "N drop" label (was overlapping 5px; now +5px clearance; Pour still hit-tests clean).
- **Strip lines in-world:** the Wizard tip "the player's read" → "(he waits — the loud Showstopper, or the quiet Usual?)"; swept nights.js + all 7 langs for meta terms (none else).
- **Night Cap:** wider side margins (0.165→0.20) so titles clear the rolled edges; footer URL github → **elixirhour.tessa-kerk.com**.
- **N1 two-shots WIRED (option A):** the Hog-crashes-in (b2) and Wizard-performs-at-the-Knight (b3) openings are now Layout-5 **duo beats** (verbatim dialogue, camera only); the rules/order + pour follow as the solo serve beat. Verified: both render as two-shots, the duo→solo→brew transition works, N1 walks clean.
- **⚑ FLAG — N2 "Knight and Princess talking together" NOT wired: the content doesn't exist.** N2 opens with a SOLO Princess visit (Sage + Princess only; the Knight has no lines in it). A two-shot needs both characters' lines — I won't invent the Knight's dialogue. Tessa: either add the Knight↔Princess exchange to the N2 opening (and I'll stage it), or point me at the beat you meant.
- **Re-shot the four captures** on v23 (mixer-nudged serve, new art).

**ROUND 9 (08-07) — pre-baked scenes, build v22. The compositing war is over.** Tessa hand-grounded the whole cast; §12 rewritten (new canon block). This DELETED more than it added.
- **Solo serve = pre-baked full scenes.** Copied her 20 `Serve Scenes (hand-grounded)` (2048×1143 → WebP q88, ~2.8 MB total) into `assets/scenes/serve/`. `setServeCustomer` is now a whole-image **cross-fade** between two stacked `#ms` layers (identical backgrounds → the bar stays dead still, only the character dissolves, ~160ms). **DELETED the entire solo compositing path**: `#serve-char`, `#serve-shadow`, all size/base/shadow/head/groupScale tables in `cast.js`, the anchoring maths, the per-Night window/tap overlays (now a subtle whole-scene filter). `cast.js` is now just colour/pronoun/head-fraction + expression→basename. Princess Arrival renamed to the `Princess - Arrival` convention. Verified: all 20 scenes load, cross-fade works, bubble clears every head incl. P.E.K.K.A's horns, no console errors, N1 walks end-to-end clean.
- **Group finale = the ready-made `Group Scene V2`** (all five hand-grounded, one image, WebP 191 KB). Deleted `placeGroupChar` / `groupComposite` / the group-row placement. Bubble anchors above the speaker from measured head positions.
- **Two-shot (Layout 5) rebuilt in-engine** from the grounded cutouts (20 → `assets/cutouts/`, WebP q90+alpha, ~1.5 MB): backdrop = the LEFT character's serve scene; RIGHT character = their cutout masked to the character column + shifted onto the same counter (identical background → the wood seam vanishes; proven seamless). Diagonal split is a **lighting treatment** (dims the silent half), never flat panels. Either side cross-fades expression. Wired to N3's existing P.E.K.K.A+Hog duo.
- **Scene transitions:** expression/character swaps cross-fade (solo + duo); a layout change fades the new screen in; `prefers-reduced-motion` drops the drift.
- **WebP sweep DONE** (mandatory before the launch zip): 20 scenes + Group V2 + 20 cutouts, zoom-QA'd (counter edge + shadows survive). N3 strip wording **confirmed** by Tessa.
- **Re-shot the four marketing captures** on the new art (`captures/serve-screen.png`, `serve-portrait`, `group-rally`, `split-duologue`) — share-quality; the case study / deck / landing inherit these.
- **⚑ FLAG — N1 two-shots (b2 Hog, b3 Wizard) NOT wired.** Those beats are two-character *brew* visits (the Knight interjects before, during AND after the pour), so staging them as Layout 5 needs either mid-visit layout switching or restructuring verbatim dialogue + the brew flow — a staging decision on signed-off content that's Tessa's call. The duo screen is READY. Options: (a) confirm a split point and I wire it; (b) Cowork pre-bakes the specific two-shot scenes (list combos here). The N3 duo already demonstrates the format live.
- **⚑ Cleanup deferred:** the old sprite pack (`assets/characters/*.png`, 1159×1440) + the V3 master scene are now UNREFERENCED but left in place (delete-last rule) — safe to prune in a cleanup pass.

**PLAYTEST ROUND 7 (08-07) — grounding rulebook, build v21. Serve + group DONE; duo scene still to build.** Tessa: the anchors pass GEOMETRY but read as floating. New GDD §12 "grounding rulebook" — the reference renders (each character generated AT the counter, `Art/Expressions/<char>/Served|Arrival/` + the `… Scene …` composites) are the measurement source of truth.
- **Contact shadows — the missing half, now a build requirement (§12).** Per-character soft pools under forearms + tankard, **`mix-blend-mode:multiply`** so they darken the wood. The earlier flat near-black overlay was invisible on the already-dark counter (proved with a red-fill test) — THAT is why every prior geometry-correct pass still read as floating. Serve (`#serve-shadow`) + group (`.g-shadow`). Widths/opacity per character in `cast.js` `shadow:{w,o}`.
- **Serve scale/anchor recalibrated to ONE contact line** (`counterY + 0.05·H`) that reproduces Knight's + Hog's approved v20 values exactly, so only the flagged cast move. **P.E.K.K.A 1.478→1.348** — her horns rendered at y≈−19 (off the top of the frame); now clear with bubble headroom; her `head` anchor 0.072→0.20 (it was above her own horn tips, impossible). **Wizard 1.585→1.52** ("down a touch"). **Princess 1.31→1.262** (bow cleared the frame top; was at y≈8). Heads: Princess 0.045→0.11.
- **Per-EXPRESSION calibration (§12 flicker rule).** Audited all five COMPLETE sets. **Princess's four faces are differently framed** (lowest-opaque 0.758→0.911) — Guard-Down Warmth (her Night-3 face) hovered ~45px above the counter and read small; each face now carries its own size+base, on-screen height matched. **Wizard Deflated** base 0.705→0.675 (hands ~30px higher in that render). P.E.K.K.A Shy nudged. **No uncovered files** — all 15 expression names in `nights.js` map to `cast.js`.
- **Group finale — same system + shadows.** P.E.K.K.A grounded + horns fit (was "way above"), Wizard down, **Princess `groupScale:1.12`** (read too small in the line-up; serve stays petite). All five plant tankard-on-wood with pools.
- **N3 consequence-gate strip bug FIXED.** The gate had no order text → `order-empty` → `display:none` (a v20 rule). Added his verbatim spoken request as `orderShort`+`nudge` so the strip stays like every other gate; ✓-suppression untouched. **Wording flagged for Tessa** ("Pour me the right one, keeper — the way I ought to go…").
- **Favicon regenerated from the 1135px hi-res source** (was 86px) — 192/512 now crisp.
- **⚑ STILL TO BUILD: the Layout-5 duo scene** (§11: inside-bar, counter foreground, diagonal *lighting* split, contact shadows, mockup-scaled) + wire the N1 two-shots (b3 Wizard+Knight, b2 Hog+Knight). Current duo is still the flat-panel placeholder. Held as the next focused piece.
- **⚑ FOR TESSA'S JUDGEMENT (live at v21):** shadow strength (currently soft), P.E.K.K.A's final scale (she's now ~Knight-sized — V3's shallow counter caps her; a deeper **V4** would let her read bigger with horns + headroom — the §12 "flag if too shallow" call), and Princess's serve-vs-group size. Proof: `captures/_vs_reference.png` (game vs reference), `_group_r7b.png`, `_princess_flicker.png`.

**PLAYTEST ROUND 6 (08-07) — DONE, builds v19 + v20.** Tessa's sixth playtest (on v18): one P0 that had her stuck, a layering bug, the anchor ruling, plus orderShort + a game favicon.
- **v19 — the unblock batch (shipped first).**
  - **P0: dose slider blocked the game — FIXED.** The Order Strip took the panel's fixed height, so the Pour button overflowed `.panel-cols` down into the full-width dose row → clicking Pour hit the slider (set it to 3 drops), and Tessa was hard-stuck at the Hog gate. `#panel-frame` height 35.5% → 41%; verified at desktop 16:9 / ~800 / ~500-portrait that `elementFromPoint` at Pour returns `brew-pour`, not `dose`.
  - **§7 layering — FIXED.** The Order Strip's opaque bar rendered over the open mixer picker, hiding options. The `.panel-cols` settle animation left a persistent `transform` (a stacking context that trapped the picker below the strip). Settle is now opacity-only; the strip is an explicit low layer (`position:relative; z-index:1`) with `pointer-events:none` text. All six spells now hit-test to the picker.
- **v20 — anchors (measured, scene-space) + orderShort + favicon.**
  - **THE FLOAT, resolved by MEASUREMENT (§12 acceptance = tankard-base-on-wood).** The anchor was already scene-space (design px through the same stage transform as the baked scene) — the real fault was the eyeballed `base` values. Each `base` is now the PNG's **true lowest opaque row** (measured with PIL), mapped so the contact plane rests mid-surface (~0.68, between the counter back edge 0.63 and front edge 0.735). Served: Knight 0.755, Wizard 0.708, Princess **0.845**, P.E.K.K.A **0.848**, Hog 0.703. Arrival: 0.810 / 0.709 / 0.689 / 0.792 / 0.670. (Tessa's rough ~0.75/0.78 for Princess/P.E.K.K.A would still overhang — P.E.K.K.A at 0.78 lands 531px, past the 529 front edge; the measured 0.848 is needed.) **Verified three ways:** PIL composite of all 5 served + 5 arrival onto the V3 scene (every tankard on the lit wood); the rig contact sheet (`captures/contact-sheet-counter.png`, all bases between the red/blue counter lines); and a **live-browser 3-viewport pass** (P.E.K.K.A worst case at desktop 16:9 / ~800 / ~500-portrait — tankard on the wood in all three, `captures/_live3.png`). ⚑ Consequence: the finale group scene sits a touch higher (same fix, applied to `placeGroupChar`) — flagged for Tessa, not a regression. The duo two-shot is unchanged/held (Layout 5, item #5).
  - **§7 orderShort — DONE.** Long gates carry a curated, hand-trimmed order line (ends "…"): Knight Usual, Hog "strong stuff" (Tessa's verbatim), Wizard Showstopper, P.E.K.K.A gentle, Knight "long look", Hog "easy one", the "make me what you'd make the Knight", Princess N3. The strip shows the short line; a tap opens a **full-request popover that grows UPWARD over the scene** (never reflows the panel — a reflow would re-open the Pour-overflow P0). No-nudge consequence beat → strip hidden (no blank bar). Rendered via `fmt()` so canon terms localise; the prose is English-with-content (translated at the content milestone, not invented here) — **flag for Tessa.**
  - **Game favicon — DONE.** Generated 16/32/48 (transparent drop for tabs) + apple-touch 180 + manifest 192/512 (drop on a dark purple glow plate, since iOS/Android render transparency as black) from `Art/UI/Elixir Drop Icon (square cutout).png`; wired into `index.html` head + a `manifest.webmanifest`. ⚑ Source art is only 86px, so 192/512 are soft — Tessa could supply a higher-res drop for crisp large icons.

**PLAYTEST ROUND 5 (08-07) — SUPERSEDED BY ROUND 6, build v18.** Tessa's fifth playtest + new GDD §7/§12.
- **Master Scene V3 (deep counter, §15) — SWAPPED IN.** `#ms-img`/`#g-img` → the V3 webp (counter top 63.0%); `counterY` re-based to 0.63 / portrait 0.403 in `stage.js` + `screens.js`. Princess (base 0.70) and P.E.K.K.A (0.73) dropped from the round-4 interim to their true values. **Contact sheet rendered for Tessa's judgment (the gate):** all five plant on the deeper counter; against the lit-surface front edge my measurement shows Knight/Wizard/Hog tankard bases clearly on the wood but Princess/P.E.K.K.A right at/just past it (their poses hold the tankard ~110px below the elbow, more than even V3's band). Per §12 "tankard-floats = wrong" this is Tessa's call — nudge those two up ~15px or accept. `captures/contact-sheet-counter.png`.
- **§7 brew gating — DONE.** The mixing panel is LOCKED (dimmed, non-responsive) outside a brew beat and unlocks with a settle animation when the customer orders; a mix is never silently reset; tapping the locked panel plays a gentle in-voice line (`brew.locked` ×7: "Let them finish — the cup can wait.") via toast — never a dead click; a brew beat announces itself (panel unlock + Order Strip). Verified: locked through dialogue → unlock on order → re-lock after pour.
- **§7 Order Strip — DONE.** The bar above the mixing panel now carries the customer's order in their own words (from the gate's `nudge`), persistently, with the (i) at its right end; hidden outside brew beats. Verified showing the Wizard's and the Knight-tutorial's actual request lines.
- **Bug — Wizard "I heard that, you know." stall RESOLVED.** It wasn't a broken advance — the pre-§7 always-active panel made "am I meant to brew now?" ambiguous. With the locked/dimmed panel the answer is unmistakable (you tap to advance; the panel lights only when he orders). Verified the whole beat stays locked until the gate.
- **Staging approved (N1 b3 Wizard+Knight, N1 b2 Hog+Knight)** and **the deferred items** (Layout-5 two-shot scene, compression sweep, brew-strip living-art, re-shoots) wait on Tessa's contact-sheet judgment (it gates the anchor/re-shoots).

**PLAYTEST ROUND 2 (07-07) — in progress, build v10.** Tessa's approvals + a second playtest. Landed so far: the **8 wrong-pour lines are APPROVED** (final wording in `data/nights.js`, DRAFT markers removed; each hints the right cup per the new GDD §7 rule "rejection is the tutorial") and the **bubble tail seam (#9)** is fixed (tail overlaps 2px into the body → one cream silhouette). **#11 (expressions "not swapping")** — the v10 "does not reproduce" verdict was **WITHDRAWN and the bug FIXED in Round 3 (v16)**: the reproduction was a choice-branch expr swap leaking into the trunk (the verification had tested setup, not the live post-choice line-advance path). See the Round 3 block below.
- **IN-GAME MENU SUBSYSTEM — DONE (build v11, §11).** New `src/hud.js` (`window.HUD`) + wiring: a **menu button** top-right (also Esc when nothing else is open) opening a cream **pause tray** — Resume · The Tome · Settings · Save & Quit + the line *"The bar keeps your place — progress saves itself."*; a persistent **Tome button** bottom-right (the new `Tome Icon (cutout).png` → 17 KB WebP) that **pulses** once when a recipe/Ledger/Herald lands, T-key still opens it; a **visible autosave quill** flourish at each Night boundary + Save & Quit; the **title's Continue names the place** ("Continue — Night 1: Opening Time"); the two **onboarding beats** (the Usual records → Sage writes it in the journal + Tome pulse + chip "The Tome — Sage's bar journal (T)"; the Knight's visit ends → chip "The Knight has a page in the Ledger now." + pulse; chips dismiss on any input); and a **continue ▼** in the active bubble while a line waits. HUD shows only on serve/group/duo. Verified in real Chrome (tray, Save & Quit → named Continue, both onboarding beats, Esc/T, pulse, quill; console clean). **Two flags:** on the *serve* screen the Tome button rides just above the mixing panel (the panel owns the bottom-right corner there) rather than the exact corner — reads fine, flag if you want it elsewhere; and the menu strings are **EN-only for now** (fall back via `t()`) — the 6-language fill is a follow-up.
- **CAPTURE RIG + #7 FLOAT + #8 BUBBLE — DONE (build v13).** **Capture rig** `game/tools/capture.py` (Playwright headless Chromium at exact 1280×720 / 720×1280 viewports → the scale-to-fit resolves to scale 1, so screenshots are pixel-exact native design space, no letterboxing). It writes to the workshop `../captures/`, produces the four marketing shots + a 5-character counter contact sheet, and is **dev-only — exclude `tools/` from any game deploy** (Python, not referenced by the game; needs `pip install playwright` + `playwright install chromium`). **#7 float FIXED:** the sprites' content-bottoms sit at inconsistent heights (Knight/Wizard/Hog rested at the counter *edge*; Princess/P.E.K.K.A sank ~40px), so a per-expression **`drop` fraction** (new field in `data/cast.js`, applied position-only in `Screens.setServeCustomer` — no rescale, sizes preserved) now plants every mug at one contact line (~16px overlap). Measured off the rig; proven with the contact sheet (`captures/contact-sheet-counter.png`) + native landscape/portrait renders. **#8 clamp:** a long line that would push the bottom-anchored bubble off the stage top now pins at `top:8` and grows downward (verified on the Knight's longest Night-1 line).
- **ROUND COMPLETE — build v15 (07-07).** All remaining items of this round landed: **#10 dose-slider own row** (panel restructured — `.panel-cols` wraps the 3 columns, the dose slider is a full-width row beneath, hidden while brewing; before/after proven on the rig, verified landscape + portrait); the **master-scene per-Night accent overlays** restored as `#ms-window-fx` (soft-light window-cool) + `#ms-tap-fx` (screen-blend tap-glow) ramping per `#stage.night2/night3` (proven distinct N1→N2→N3 on the rig); the **group/rally screen swapped onto the baked master scene** (`#g-img` cover, cast re-anchored to counterY 0.719 landscape / 0.46 portrait with the per-expression `drop` so all five plant tankard-on-wood; N3 brightness/saturation accent); then the **four re-shoots** (serve-screen, serve-portrait, group-rally, split-duologue) re-rendered via the rig into `../captures/`. (Menu 6-lang localisation — DONE, build v12. #11 expression swaps — verified non-reproducing, no fix.) **Committed `000fb94`; the push to `origin/main` was blocked by the auto-mode classifier (wants a feature branch/PR) — the commit is safe locally and needs Tessa/an interactive push. Round not closed until Tessa runs her acceptance playthrough on the v15 stamp.**
**PLAYTEST ROUND 4 (08-07) — IN PROGRESS, build v17.** Tessa's fourth playtest (on v16). Done + verified this build; a hard STOP flagged for Cowork; some items deferred.
- **#2 expression size-jump — FIXED.** The renderer scaled each sprite by its per-expression content bbox (`imgH = charH/span`, span varied 5–24%), so faces resized on swap. The five sprites are FACE-SWAPS on one registered 1159×1440 canvas (verified by a flicker overlay — head/hood/shoulders pixel-identical, only brows/mouth differ), so `data/cast.js` is re-parameterised to a per-character constant `size` (+ `base`, `head`) — swapping a face now produces zero size/position jump (flicker test: diff bbox starts below the head-crown). `screens.js` serve/group/duo all use the new model.
- **#1/D elbow anchor — 3/5 shipped, 2/5 STOP→V3.** Anchor target changed from lowest-point to the FOREARM/ELBOW underside on the counter-top (per-character `base`). Knight/Wizard/Hog plant true elbow contact with the tankard above the front edge on V2. **Princess & P.E.K.K.A hold the tankard forward/low → planting their elbows pushes the mug PAST the counter's front edge** (V2's lit band is only 0.667→~0.735 ≈ 49px). Per Tessa's directive I STOPPED rather than compromise: their true values (Princess base 0.70 / P.E.K.K.A 0.73) are held at a non-overhang interim (0.75 / 0.80) PENDING **Master Scene V3 (counter ~63%)** — evidence in `captures/contact-sheet-v3-needed.png`. When V3 lands, drop those two to the true values.
- **A Night 1 expressions static — FIXED.** Not an engine bug: Night 1 beats set ONE `expr` at setup and had NO per-line swap cues (Night 2/3 do). Authored cues onto the three emotional beats — verified the Wizard fizzle → **Deflated** → recovers to Boastful (acceptance test), Knight Weary&Wistful at the reflections, Hog Warm Grin/Big Laugh/Quiet&Wistful.
- **#3 pagination — FIXED (incl. a since-round-3 real-play bug).** Split preference is now (1) before a bracketed stage direction, (2) sentence boundaries; the finale Knight speech breaks at "(the room turns)" and "(he raises the cup)" into 3 pages. **Root real-play bug:** `playBeat` called `Dialogue.start` (→ the paginator's line measurement) BEFORE `show(screen)`, so a hidden screen measured `offsetHeight 0` → 1 line → never paginated in real play (my round-3 tests used show-first setups and missed it). Swapped to show-then-start. Bubbles also now anchor to the character's head-top (`head` fraction) so they never cover the face — P.E.K.K.A's butterfly speech clears her horns.
- **#4 chip + terminology — FIXED.** The onboarding chip now sits directly beside the Tome icon (top-right) with its tail pointing at it. Every Tome reference uses the language's Tome term (en "the Tome", ko 일지, ja 手帳, de Die Chronik, es El Tomo, fr Le Tome, pt O Tomo) — chips, tooltips, and Sage's onboarding line all swept; "journal"/"book" removed from UI copy.
- **E button padding — FIXED.** Title/rest-screen buttons got 1.4em horizontal padding + wider tray; "Continue — Night 3: Last Call Before the Crown" wraps with clear margins, no edge-hugging.
- **B staging audit — DONE, list for Tessa's approval.** Multi-character beats currently solo Layout-1: **N1 b3 (Wizard+Knight, the fizzle/interrupt beat — the GDD's named first conversion)** and **N1 b2 (Hog+Knight)**. Both are Night 1 (satisfies "a non-Layout-1 staging before Night 3"). Awaiting Tessa's OK before staging as Layout-5 two-shots.
- **DEFERRED:** C (the Layout-5 two-shot scene rebuilt as inside-the-bar — a substantial build; the N3 P.E.K.K.A/Hog duo also needs V3 for P.E.K.K.A), #5 the M8 compression sweep (large), #6 the optional living-art brew strip, and the four re-shoots (the anchors move again with V3, so re-shooting now would be stale). These wait on V3 + Tessa's staging approval.

**PLAYTEST ROUND 3 (07-07) — DONE, build v16.** Tessa's third playtest (on v15): two P0s, a reopened bug (with photo evidence), and GDD-locked design changes. All landed + verified on the stamped build (console clean):
- **P0-1 — Save & Quit → Continue "reset to the beginning" — FIXED (root cause found by real-click reproduction).** The save persisted only the BEAT index, never the position *within* the beat's script, so `Dialogue.start()` replayed the whole visit from its first line — a mid-conversation/mid-brew save rewound the current visit (for an early beat, to the game's opening). Fix: a **within-beat resume cursor** — `Dialogue.cursor()` persists the remaining queue (current pause entry re-included) into the save (v5); `Dialogue.resume()` rebuilds the beat from there; `Game` calls `resume()` instead of `start()` when a cursor matches. Verified: mid-conversation, mid-brew, real page-reload → Continue, and v4-save fallback (no cursor → per-beat, no crash) — all land exactly where saved.
- **P0-2 — mixer picker ran off-screen (Poison unreachable) — FIXED.** `.picker` opened downward from a slot low in the panel. Now it opens **upward** (bottom-anchored) with a `max-height`+scroll safety net; verified all six spells reachable at the worst-case slot in BOTH orientations.
- **#11 — expression swaps (REOPENED with photo evidence: Wizard wore Boastful Grin in his Deflated beat) — FIXED.** Not a key mismatch (prior verdict withdrawn). Real defect: an expr set **inside a choice reply** leaked past where the branch rejoins the trunk (Wizard puff→Boastful bled into the bare closing line). Fix: a **branch-scoped restore-sentinel** (only fires on choice-reply expr swaps — the only such case in the whole script is the Wizard N2 puff branch; every *persistent* swap is a trunk line, untouched) + authored the closing line `expr:"Warm Smile"`. Verified: Wizard reads Deflated→Boastful→Warm Smile, P.E.K.K.A's Calm→Shy persists (the regression an adversarial verifier flagged is avoided). Proven with an in-game 6-face contact sheet (`captures/expression-proof.png`).
- **Bubble pagination (§11) — DONE.** A bubble never exceeds ~4 lines; long lines split at sentence boundaries into pages advanced by the same tap with the ▼, scripts verbatim (engine paginates, stage directions kept whole). Verified on the Knight's two-kings speech (516 chars → 3 pages of 3/4/2 lines; no longer covers his face).
- **Tome staged reveal (§11 reworked) — DONE.** The icon does NOT exist until the first recipe records; then it settles in (top-right, on a **cream/gilt plate**, directly below the menu button) with a held glow + a tap-first chip, and the next open lands on the Brew Book. The Knight's first Ledger page glows it again → next open highlights the Ledger ribbon. After both, normal pulse. Reveal derived from the save (survives reload, no re-animation). Chips tap-first in all 7 languages.
- **Master Scene V2 swapped in (§15).** `#ms-img`/`#g-img` → `Serve Screen - Master Scene V2 (tall counter).webp` (counter top raised 71.9%→**66.7%**, a deeper band). `counterY` re-based to **0.667** (landscape) / 0.427 (portrait) in BOTH `stage.js` and `screens.js groupComposite`. The existing per-expression `drop`s already normalise every mug to one contact line (measured uniform 496px on the rig), so on the deeper band the cast plants with wood visibly below arms/tankard — the V1 spill is gone. Proven with the 5-char contact sheet + serve/portrait/group renders.
- **Four captures re-shot** on v16/V2 into `../captures/` (+ the expression proof + contact sheet).

- **Master scene V2 (Fix 5 / §15 DONE):** `#ms`/`#g-img` are the baked V2 image (`Serve Screen - Master Scene V2 (tall counter).webp`, 122 KB), cast anchored by torso-base to the **66.7%** counter (`stage.js`/`screens.js` counterY 0.667 / portrait 0.427). Per-Night accents + the group swap remain (from v15).
- **Consequence brew (N3 Knight):** shows the recipe name but **never a ✓** (arms a never-true order predicate) so it can't leak the §8 "right" answer — flag if Tessa wants different.

**NEXT: Milestone 8 remainder** — the **asset compression sweep** (Night Cap scroll 1.6 MB, nav icon 569 KB, the logo — compress every sprite/scene, per the title-bg 7.2 MB → 233 KB precedent). *(The group/rally master-scene swap is done, v15.)*

**NEEDS TESSA:**
- **M7 audio drop:** the looping tavern-ambient track(s) → `game/assets/audio/` with the filename set in `data/tome.js` `SONGBOOK[].file`; the three SFX → `game/assets/audio/sfx/{brew-fizz,mug-clink,page-turn}.mp3`. The system runs clean and silent until then — it's a pure file-drop, zero code.
- **6 canon terms stay English** (no official CR term verified — never guessed): ko Freeze · es Dark Elixir · fr Heal · fr Zap · pt Heal · pt Freeze. If you have an official Supercell source for any, add its `term.<name>` key to that language block in `strings.js` (audit table below).
- **`GAME_URL`** in `src/nightcap.js` is a placeholder — swap for the hosted landing-page URL at deploy (part of the canonical-URL swap; see Cross-session requests). CJK canvas font on the Night Cap parked as optional polish.
- **Content Tessa owes** (engine ready): bespoke wrong-brew lines (`wrong:[…]`); a read-aloud pass on all three Nights. The 3 Night Cap quotes are **approved**.

## Cross-session requests
*Territory rule (06-07-2026): this session owns everything except `site/`; the landing session owns `site/`. Post asks here instead of editing the other territory. **Check this section at the start of every work block.***

**✔ RESOLVED (R20, 09-07) — Hog Tome spread re-shot FULLY UNLOCKED.** The game session re-captured `../captures/ledger-hog.png` (1280×720) + `ledger-hog-portrait.png` (720×1280) with the Hog Rider page fully unlocked — both pages full, all three stages of bio, all margin notes landing (4px gap), and the green *"With the noise off: the Hog's getting old…"* confessional note present. Used `window.DEV_LEDGER_STAGE=3` + Hog marked met, driven through the capture rig (Playwright, native design scale). **Landing session: the fully-unlocked captures are in `../captures/` under the standard names — WebP them into `site/assets/img/ledger-hog.webp` + the mobile crop and finish the pass.** Original request kept below for the record.
> **(orig ⚑ from the LANDING session, 09-07):** the fresh `../captures/ledger-hog.png` (+ `ledger-hog-portrait.png`) was shot at an **earlier unlock stage** — right page nearly empty, green note missing. Per Tessa's call the landing kept the FULL spread rather than regress an approved surface, and asked the game session to re-shoot fully unlocked (same crop, 4px-gap arrows). Everything else in the 09-07 landing pass (real serve captures, 3 full-bleed breakers, GitHub footer link, auto-hide indigo scrollbar) is done and verified — only the Tome image waited on this.

**→ For the COWORK-side Claude (WebP encoder), from the GAME session — UI-PNG weight sweep (08-07-2026, R12):** the game ships ~34.7 MB of PNG on the built UI surfaces (the only assets still uncompressed after the round-10 scene/cutout WebP sweep). No encoder in the game session's environment. Please bake **WebP twins at the SAME path** (e.g. `assets/ui/Tome Spread.png` → `assets/ui/Tome Spread.webp`), keeping alpha and current pixel dimensions; the game session will swap the `src`/`url()` refs (and bump `?v=`) when they land. **Cards are locked art — use LOSSLESS (or ≥q92) so the pixels are unchanged**; the rest can be lossy q86–90. Targets (all under `game/assets/`):

| Source | px | now | target |
|---|---|---|---|
| `ui/Tome Spread.png` | 2752×1536 | 4.44 MB | ≤ 1.2 MB (lossy ok) |
| `ui/cards/Knight Card (cutout).png` | 1525×2048 | 4.12 MB | lossless ≈ 1.2 MB |
| `ui/cards/Hog Rider Card (cutout).png` | 1698×2359 | 3.81 MB | lossless ≈ 1.2 MB |
| `ui/cards/P.E.K.K.A Card (cutout).png` | 1689×2346 | 3.85 MB | lossless ≈ 1.2 MB |
| `ui/cards/Wizard Card (cutout).png` | 1551×2155 | 3.76 MB | lossless ≈ 1.1 MB |
| `ui/cards/Princess Card (cutout).png` | 774×1076 | 1.33 MB | lossless ≈ 0.4 MB |
| `ui/Brew Panel - Spell Drop (4x5).png` | 1856×2304 | 3.83 MB | ≤ 1 MB |
| `ui/Brew Panel - The Stir (4x5).png` | 1856×2304 | 3.85 MB | ≤ 1 MB |
| `ui/Brew Panel - The Pour (4x5).png` | 576×720 | 0.43 MB | ≤ 0.2 MB |
| `ui/Night Cap Scroll (cutout).png` | 1100×1569 | 1.62 MB | ≤ 0.5 MB |
| `ui/Songbook Phonograph (cutout).png` | 905×1424 | 1.51 MB | ≤ 0.5 MB |
| `logo/Elixir Hour Logo (transparent).png` | 1408×752 | 0.89 MB | ≤ 0.3 MB |
| `ui/Nav Arrow (icon).png` | 1786×536 | 0.57 MB | ≤ 0.15 MB |
| `ui/Tankard Icon (cutout).png` | 655×614 | 0.54 MB | ≤ 0.2 MB |
| `ui/Card Select (icon).png` | 532×300 | 0.16 MB | ≤ 0.08 MB |

Leave `assets/share/night-cap-share.png` as PNG (og:image — some scrapers reject WebP). **Do NOT delete the ~36 MB of superseded source PNGs in `assets/characters/` + the two `assets/scenes/*.png`** (delete-last rule) — they're unreferenced by the game; please **exclude them from the deploy zip at the Tools/packaging level** instead. Full context: `game/DEPLOY-COMPRESSION.md`.

**⚑ PENDING (07-07, updated) — landing ch9 "Behind the bar" flag flip still blocked:** the **case-study page IS now built** (`site/case-study/`, see (3c) below), so one of the two link targets exists; `site/js/flags.js` `BEHIND_THE_BAR` stays **OFF** until the **GTM deck PDF** (`/assets/elixir-hour-gtm.pdf`) is also deployed AND both return 200 on the live domain. Flip procedure in the (3b) bullet below.

**⚠️ FLAG — accidental cross-territory commit (landing session, 07-07):** commit `5fc1060` ("Landing page round 5…") **unintentionally included the game session's files** — `css/main.css`, `data/nights.js`, `index.html`, `src/{boot,dialogue,hud,screens,state}.js`, `strings/strings.js`, `assets/ui/Tome Icon (cutout).webp`, and a PLAN.md edit. Cause: those were already `git add`-ed (staged) in the shared index when the landing session ran `git add site/… && git commit`, so they rode along. **No content was lost** — that snapshot is exactly whatever the game session had staged, and it's pushed. **Game session: please verify the v10 build is coherent** (it's your own staged state, just committed under a landing-page message). Not reverting — "flag, don't fix", and a shared-history rewrite while you're active is riskier than a mixed commit. The landing session will `git reset` the index before every commit from now on to prevent recurrence. Apologies.

**→ For the LANDING session (`site/`), from the game session — deploy-time canonical-URL swap (06-07-2026):** the share plumbing that landed in `site/index.html` (og:image / twitter:image → `assets/img/night-cap-share.png`, plus `favicon-48.png` + `apple-touch-icon.png` at the site root) uses RELATIVE paths and has no `og:url`. **On deploy, set the ABSOLUTE `og:url` + absolute `og:image`/`twitter:image` URLs** to the canonical domain (Facebook/LinkedIn require absolute; Discord/X accept relative but absolute is safest). This PLAN entry is the authoritative record of that flag — the inline "set the ABSOLUTE og:url…" comment in the `site/index.html` head is now yours to keep or remove. `site/assets/img/night-cap-share.png` is the pre-rendered Night Cap (1200×1500, 4:5, portrait) — re-export it from the game's Night Cap if the card art changes (the master lives at `game/assets/share/night-cap-share.png` and `../captures/night-cap.png`). Note the Night Cap is portrait 4:5, so X/Twitter will centre-crop it in a `summary_large_image` preview; if you want an uncropped landscape link-preview, ask the game session for a dedicated 1200×630 OG crop.

**→ For the COWORK-side Claude (WebP encoder), from the landing session — image-weight conversion for Lighthouse ≥ 90 (07-07-2026):** there is no WebP/AVIF encoder in the landing session's environment, and the card + character PNGs are the mobile-Lighthouse byte bottleneck. Please convert the files below to **WebP** (keep alpha where the source is PNG) and write each to the **same path with a `.webp` extension** (e.g. `site/assets/img/cards/Knight.png` → `site/assets/img/cards/Knight.webp`). Keep the current pixel dimensions unless noted (they're already ~1.5× the displayed size; for retina-crisp, ≥2× the "displayed max" is ideal). The landing session will swap the `src`/`url()` references to `.webp` when they land, then run Lighthouse. Targets:

| Source (under `game/site/assets/`) | Alpha? | Displayed max (CSS) | Target |
|---|---|---|---|
| `img/cards/Knight.png` `Hog.png` `Wizard.png` `Princess.png` `Pekka.png` | yes | 244×334 (190×266 mobile) | ≤ 80 KB each |
| `img/knight-full.png` | yes | ~305×455 (ch2 figure) | ≤ 140 KB |
| `img/sage.png` | yes | ~205×445 (Sage beat) | ≤ 100 KB |
| `img/logo.png` | yes | 460×246 (hero) | ≤ 70 KB |
| `img/tankard.png` | yes | 172px (brew) / 46px (cue) | ≤ 35 KB |
| `img/bar.jpg` | no | full-bleed hero, ≤1440w | ≤ 120 KB |
| `img/ledger-hog.jpg` | no | 1060×591 (ch6 Ledger) | ≤ 90 KB |
| `shots/frozen.jpg` `spell.jpg` `hog.jpg` | no | ~300×201 (arena tiles) | ≤ 40 KB each |
| `shots/serve.jpg` `serve-portrait.jpg` | no | ~600w / ~206w (play-it) | ≤ 35 KB each |

**Skip these (unused / superseded — do NOT convert):** `img/knight-arrival.png` (843 KB, replaced by `knight-full`), `img/tome.jpg` (replaced by the Ledger capture), `img/ledger-hog-rider.png` (1.6 MB source master, replaced by `ledger-hog.jpg`). The landing session will decide whether to drop them.

**✅ WebP conversion DONE (07-07):** all `site/` references swapped to `.webp`; the superseded original PNGs/JPGs deleted from `site/assets/` (kept `night-cap-share.png` + the favicons, still referenced by the meta tags). Site image payload down ~90% (cards 452–495 KB → 26–31 KB; knight-full 1081 → 52; sage 723 → 47).

**✅ LANDING M3 DONE + SIGNED OFF BY TESSA (07-07, later) — pipeline fixed, mobile framing, perf, canonical-URL swap.** All Milestone-3 items done; Tessa signed off the framing with one follow-up fix applied (ch6 single-page Ledger crop, below). **Then reopened for new scope — chapter 9 "Behind the bar" (see (3b) below).**
- **Screenshot pipeline — the intermittent hang is a harness-level fault in the preview MCP's *pixel* capture, not the page.** Confirmed: `preview_eval`/`preview_snapshot`/`preview_inspect` work, but `preview_screenshot` times out even on a stripped 1-box page. **Working pipeline (use this, not `preview_screenshot`):** drive the system Chrome via **Playwright + Pillow** — `python.exe` 3.14 + `playwright` 1.61 are on this machine; launch with `channel="chrome"` (**no `playwright install` needed**). Reproduce any scroll frame with the engine's own math `scrollY = wrap.offsetTop + p*(wrap.offsetHeight - vh)`; inject `html{scroll-behavior:auto}` first or a long `scrollTo` lands short; `is_mobile=True, device_scale_factor=2` for phone renders; `window.HeroFX.stop()` to freeze. Reusable harness preserved at **`Tools/site-capture/`** (`cap.py` captures, `measure.py` fit-checks, `perf.py` throttled CWV, README with the method). See also the cross-session note below for the game rig.
- **(1) Mobile portrait framing DONE.** Bespoke ch2 portrait: the 16:9 stage becomes a tall (66vh) centre-crop of the taps+counter with the Knight re-placed as a real presence and the caption in the dark below — all coordinate-preserving (stage stays a full 16:9 box, sides clipped by the pin, so the glow/arrow coords are untouched; only Knight/bubble/note re-placed). ch4 brew + ch5 herald shrunk on mobile so their payoffs (the ✓ "The Usual" + caption; the ⚗ ripple line + "you poured it" note) clear the 100vh pin instead of clipping. **ch6 Tome (Tessa's follow-up):** the full Ledger spread squeezed into a phone read as accidentally cut off — a phone now gets a deliberate single-page crop (the right page: bio + Sage's green pinned note, with the page edges + ribbon tabs kept so it reads as one page of a book) via `<picture>` → new asset `site/assets/img/ledger-hog-rider-mobile.webp` (660×610, 29 KB); desktop keeps the full spread. Added `width`/`height` to the previously-unsized `<img>`s (CLS guard). **Post-signoff refinements (07-07, from Tessa's review):** (a) **ch2** — the Knight is scaled up and cropped **knee-up**, feet off the band's bottom edge (`.bb-stage{overflow:hidden}` on mobile; crop lands between joints), and the ground shadow removed — this puts the floor contact + the stool/perspective clash out of frame; bubble stays above his head, lantern + Sage's note unchanged. (b) **ch6** — the crop re-cut wider so all four ribbon bookmarks show in full with wood margin on the right, and the gutter + a facing-page sliver on the left → reads as the right page of an open book (asset re-saved 500×608 → 660×610). **All changes are inside `@media (max-width:640px)` / mobile-scoped selectors — desktop (signed off) re-checked untouched.** Verified at 360×780 and 390×844 against real captures.
- **(2) Perf — the record (Tessa: measured CWV is sufficient, no official CLI needed).** Measured under Lighthouse's mobile throttle (4× CPU, slow-4G, 360×780@2×): **FCP ~1.40s · LCP ~1.56s · TBT ~10ms · CLS 0.014 — all in the GOOD band**; ~509 KB initial transfer / 15 requests / 228 DOM nodes. Maps to est. Lighthouse Performance ~98–100 (≥90 comfortably). These measured values ARE the inputs to Lighthouse's score; the official Node CLI isn't installable here without elevation and Tessa has accepted the throttled-CWV measurement as the perf record.
- **(3a) Canonical-URL swap DONE in `site/index.html`.** Domain is **`https://elixirhour.tessa-kerk.com/`** (static bundle: landing at `/`, game at `/play/`). Absolute `og:url` + `<link rel=canonical>` + `og:site_name`. **OG image upgraded (07-07):** `og:image` + `twitter:image` now point at a dedicated **1200×630 landscape hero** `site/assets/img/og-share.jpg` (Cowork-composed: master scene + Knight at the counter + logo) so a share card fills the frame instead of the portrait Night Cap floating half-empty; `og:image:height` corrected 1500→630. These two are RELATIVE (`assets/img/og-share.jpg`) on purpose — the deploy script's now-path-agnostic `og:image`/`twitter:image` regex prepends the canonical host at bundle time (verified: bundle → `…tessa-kerk.com/assets/img/og-share.jpg`); canonical + og:url stay hard-absolute. Night Cap PNG kept (still the game's own `game/index.html` share meta + a standalone asset).
- **(3b) Chapter 9 "Behind the bar" ADDED (07-07, brief §2 ch9 — new scope).** The case-study/deck maker-voice section the page was missing, inserted between the editions strip (ch8) and the footer (now **ch10**). Cream **parchment panel** on the night ground in the §0 visual system (Baloo 2 head, Cormorant serif lede, double-rule masthead, purple accent) — a deliberate register switch from game voice to maker voice, with **one** Sage-green Caveat margin note ("read this one first") whose hand-drawn arrow **leaves the note and lands ON the case-study button** (§0's two locked marginalia rules, 07-07: a note is an add-on not a repetition; the arrow flows note→target). Two CTAs: **"Read the case study"** (purple primary) → `/case-study/`, **"Download the GTM deck (PDF)"** → `/assets/elixir-hour-gtm.pdf` — the **secondary/deck CTA is SAGE** (#9CAF88 fill / #6B7F5C border+hover / charcoal text, ~6.9:1 contrast = AA, the one place the page uses the sage accent), carries `download` (root-absolute placeholders); plus a small **"The BrawlHouse — the first format →"** link → `https://brawlhouse.tessa-kerk.com/`. **Ships behind a one-line feature flag:** `var BEHIND_THE_BAR = false;` at the top of new `site/js/flags.js` — the section carries a `hidden` attr, the flag removes it; default OFF so the not-yet-deployed case-study/deck links never show live (verified: `display:none`, zero rendered height, no layout gap, no-JS keeps it hidden). Reduced-motion safe (static); verified at 360/390 + desktop, console clean. **Copy is Tessa's final verbatim** (07-07 review). Review-round fixes all in: (1) the green-note arrow now flows note→button; (2) the deck CTA switched cream→sage; (3) final copy + the "The BrawlHouse — the first format →" link line. **✅ SIGNED OFF by Tessa (07-07) as rendered, desktop + 360 — nothing further on the section until the flip.** **⚑ STANDING FLAG STATE — stays OFF until BOTH `/case-study/` AND `/assets/elixir-hour-gtm.pdf` are actually deployed on the live domain. The flip, when they land:** (1) set `BEHIND_THE_BAR = true` in `site/js/flags.js`; (2) re-verify BOTH hrefs return **200 on the live domain**; (3) commit + push; (4) Cowork builds a fresh deploy zip for the developer. Do NOT flip before both files are live. Files: `site/index.html`, `site/css/site.css`, new `site/js/flags.js`.
- **(3c) Case-study page BUILT (07-07, brief §1 — new deliverable).** So the ch9 "Read the case study" button has a real destination. Lives at **`site/case-study/`** (`index.html` + `case-study.css` + `img/` placeholders), ships at `elixirhour.tessa-kerk.com/case-study/`. **Copy is Tessa's FINAL verbatim** from `Elixir Hour - Case Study (Final Copy).md` — typeset by a build script (`Tools/`-style transform in the session scratchpad, `buildcs.py`) so the words are copied, never retyped; `[N]` + the (#) Play hrefs are the only fill-later slots (Play → `/play/`). Design = the §0 system in code: night ground, a centred cream **parchment reading column** (~760px), purple "CASE STUDY" kicker, Baloo 2 heads, **Cormorant Garamond body** (run-in `<strong>` lead-ins forced to a real bold via `font-synthesis:weight` at 800, since the 600 file has no bold), Herald double-rule, Nunito UI. The **two ✎ notes are Sage's green Caveat marginalia**: on desktop they float in the night side-margin with a hand-drawn arrow flowing FROM the note ONTO the underlined target phrase (§0 rule; note 1 → "ten rounds of back-and-forth", note 2 → the page-turn sentence); at <1120px the margins collapse and each note sits above its target with a down-arrow. Four **[SHOT] figures** = thin gilt frames + Nunito captions, **named by slot** (`img/shot-{hero,ledger,brew,nightcap}.webp`) so the finals swap one-for-one; current placeholders are game captures. Two Play CTAs purple; the BrawlHouse external is sage (per ch9). OG/canonical for `/case-study/` (og:title "Elixir Hour — the case study", og:image reuses the landscape OG hero, relative → deploy-zip absolutises). **Print stylesheet = the PDF export** (Ctrl+P): white ground + dark text, buttons/nav hidden, notes stay green (inline), gilt figures scale to page width, A4 margins, URLs printed after links — verified via `page.pdf()`. Guard-rails all met: transforms/opacity only, reduced-motion static, figures lazy-loaded + `width`/`height` (CLS 0.027), **console clean**, **perf under mobile throttle FCP 1.16s / LCP 1.16s / TBT 18ms / CLS 0.027 — all GOOD → Lighthouse ≥90** (236 KB / 8 reqs / 118 nodes). The page stays **UNLINKED** (ch9 flag OFF) — it may exist at the URL, that's fine. **Swap-when-finals-land:** replace the four `site/case-study/img/shot-*.webp` (same names) with the real captures + fill `[N]`. Files: new `site/case-study/{index.html,case-study.css,img/*}`.

**→ For the GAME session — `GAME_URL` (07-07, RESOLVED by Tessa):** the Night Cap's printed URL reads the bare **`elixirhour.tessa-kerk.com`** — Tessa's call: it's a display string on a postcard, so `https://` is visual noise there. The deploy script `Tools/build_deploy_zip.py` **already sets exactly this on the bundle copy** (`var GAME_URL = "elixirhour.tessa-kerk.com"`), so the shipped Night Cap is correct with **no source change required**. If you do edit `src/nightcap.js` for source-consistency, use the bare `elixirhour.tessa-kerk.com` (NOT `https://…`) so it stays in step with the script's exact-string replace, and bump `?v=`. NEEDS-TESSA `GAME_URL` item = closed.

**→ For the GAME session — use the Playwright+Pillow capture method for the rebuilt rig (07-07, from the landing session, at Tessa's request):** you're about to rebuild `game/tools/capture.py`. Use the same reliable method the landing pass proved, and **do not touch the preview MCP's `preview_screenshot`** (it hangs at the harness level — times out even on a 1-box page; `eval`/`snapshot`/`inspect` work but pixel capture doesn't). The method: **Playwright driving the system Chrome via `channel="chrome"` — no `playwright install chromium` step needed** (avoids the browser-binary download your prior rig required); `Page`/`context` with `is_mobile` + `device_scale_factor` for exact device pixels; scroll to any frame with `scrollY = wrap.offsetTop + p*(wrap.offsetHeight - vh)` after injecting `html{scroll-behavior:auto}`; freeze animation before the shot; Pillow for any crop/encode. A working reference harness (site-flavoured, easy to adapt to game screens) is preserved at **`Tools/site-capture/`** with a README. This makes captures a solved problem for every future session — game or landing.
**Deploy-bundle note (landing):** the landing's game links (footer + CTAs, `href="../index.html"`) resolve to the game at `../` in the local preview; the deploy bundle rewrites them to `play/` (landing at bundle root, game copied to `bundle/play/`). Bundle build is **gated on Tessa's sign-off of the mobile framing** (she reviews an Artifact first) — not built yet.

**✅ SUPERSEDED — this kickoff is DONE + SIGNED OFF; see "LANDING M3 DONE" above. Left for the record only.** ~~**→ For the NEXT LANDING session (`site/`) — remaining Milestone 3, IN THIS ORDER (07-07-2026):** the landing page is otherwise signed off (all 9 chapters + the Sage beat, the grounded Knight, uniform rail cards, real Ledger, WebP). Three items remain — do them in order and don't do (1)/(2) blind:~~ **(1) Mobile portrait framing** — the pinned scenes (ch2 bar-build, ch3 rail, ch4 brew, ch5 herald) letterbox to a thin 16:9 band on phones; they need a bespoke portrait composition (reposition the Knight + the glow pools for the narrow crop, re-anchor the rail). **REQUIRES a working preview screenshot pipeline** — it was intermittently hung 06–07 Jul; start by confirming a screenshot succeeds before touching layout. **(2) Lighthouse mobile ≥ 90** — the image-weight bottleneck is resolved by WebP; run a real Lighthouse pass and confirm the score. **(3) og:url / og:image / twitter:image + `GAME_URL`** — absolute-URL swap at deploy once the canonical domain exists (`GAME_URL` lives in the game's `src/nightcap.js`; the site meta is in `site/index.html` head).

**SOURCE OF TRUTH:** `../Elixir Hour - Game Design Document.md` is the master — re-read after compacting, especially §11 (Tome/Ledger, page-turn, Songbook), §12 (art + scene), §14 (milestones), §15 (assets). This PLAN.md and CLAUDE.md track the build; **the GDD wins on any conflict**.

**LOCKED — DO NOT UNDO OR REGENERATE:**
- Character sprites: grey-background renders, already cut out and placed — **never re-key or regenerate**.
- Logo (transparent) and title background: **FINAL — don't touch**.
- Nav arrow: `Art/UI/Nav Arrow (icon).png` (3D render); points left = back, flipped horizontally = next page.
- All 5 character cards are uniform aspect (~1.37); the Knight card was regenerated to match — **no per-card special-casing**.
- Ledger = two-page spread; bio **starts on the LEFT page** and flows right (CSS-column book flow); notes in **both** margins, **no pen icons**, arrows **never cross text** (nearest edge, routed under the line); back/page-turn arrows are the 3D nav icon, **not** Sage's green hand.
- Songbook gated by progress: Sage's Favourite (start) · Rain on the Strip (end Night 1) · The Little One's Tune (meet P.E.K.K.A, Night 2) · Last Call (Night 3); **no "finale" wording** anywhere.
- Settings screen exists (7-language dropdown with live swap, AI-translation disclaimer, music/SFX toggles, sage-green About/Credits).

**KNOWN GAP:** wrong-brew reaction lines don't exist in the script yet — the dialogue engine is ready for them (`wrong:[…]` entries; currently a wrong pour repeats the request line verbatim).

Milestones from the GDD §14 (`../Elixir Hour - Game Design Document.md`). Each must be runnable in a browser before it's ticked. Design target: the Serve Screen and Ledger Page mockups in the parent folder — copy their layout maths and data shapes, rebuilt responsive and data-driven.

## Milestones

- [x] **0. Repo setup** — `game/` folder, git, locked assets copied into `assets/`, CLAUDE.md + this plan. *(04-07-2026)*
- [ ] **1. Skeleton + state machine.** Screen states (Title/loading → Herald interstitial → Serve → Tome overlay → Group/Split beats → epilogue), Night/visit sequencer, save/load (autosave + Save & Quit/Continue), responsive 16:9 stage. Data-driven from empty content files. Animated loading screen (transparent logo + Elixir-mote/ember particles, GPU-cheap, pauses when tab hidden).
  - [x] `index.html` + `css/main.css` — stage, all screen containers, GDD §12 palette vars, title/herald styled per the mockup
  - [x] `src/stage.js` — scale-to-fit 1280×720 design space (letterboxed, resize + orientationchange); exposes `COUNTER_LINE_Y` + cast anchor constants for Milestone 2's torso-base placement
  - [x] `src/state.js` — screen state machine + Night/visit sequencer walking `NIGHTS[].beats[]`; Tome as overlay, not a state; `startNight` guards non-existent nights (never persists a poisoned save)
  - [x] `src/save.js` — autosave on beat advance + Night boundary, Save & Quit, Continue; **all storage in try/catch with in-memory fallback** (a blocked/unavailable localStorage must never crash the game); `Prefs` (language/music/sfx) under a separate key so New Game keeps your language
  - [x] `src/particles.js` — title canvas: Elixir motes + amber embers, 40 particles, RAF, pauses on hidden tab, honours `prefers-reduced-motion` live (change listener, both directions)
  - [x] `src/screens.js` — title fully real (Continue reflects save state); Herald renders from data; serve/group/duo/epilogue as labelled placeholders
  - [x] `data/nights.js` / `data/cast.js` / `data/tome.js` + `strings/strings.js` — content shells with shapes locked (beats, SPELLS/RECIPES/CAST, Tome tabs, four Songbook slots); string table with `t()`, en hand-written, **UI chrome AI-translated in all 7 languages** (content strings translate with their content milestones). Classic scripts on `window.*` — **no ES modules, no fetch()** — so it runs from `file://`
  - [x] **Settings screen (GDD §11, added 04-07)** — cream-panel overlay from the title: language dropdown (native names) live-swapping every string via the tables and persisted; AI-translation disclaimer beneath; Music + SFX toggles (persisted; audio wires up in Milestone 7); About/Credits panel with the §17 disclaimers; Back/Esc close (Esc: About → Settings → Tome)
  - [x] **Transparent logo (pulled forward from Milestone 8)** — keyed from the plate original with a purpose-built flood-fill/soft-alpha tool (no Python on this machine), 125 stray islands + watermark ghosts dropped, web-optimised to 829 KB; full-res copy filed in `Art/Logo/`; CSS re-adds a soft Elixir glow
  - [x] Verified in browser (state flow, mobile/tablet resize, save→reload→Continue, storage-blocked boot, particles pause, language live-swap + persistence, Night-4 guard, clean console) → commit + push pending Tessa's go
- [x] **2. Serve screen (Layout 1), the locked two-layer composite.** Master scene (bar baked in) → no-bar character cutout on top → mixing panel overlay. Prove the composite with placeholder art first. Character placement is **programmatic**: one fixed counter-line Y and one horizontal anchor for the whole cast, each character **anchored by the bottom of the torso** to the bar line (never by the hands or the lowest pixel — lowest-pixel sinks them into the counter, hand-anchoring floats them). Hands/forearms rest *on* the baked counter with a standardised CSS contact shadow beneath. No foreground counter strip, no occlusion layer, **one counter only** (the baked one). Dialogue bubble above the speaker. **Explicit task: mobile-portrait reflow** — the serve screen stacks/rescales so character, dialogue and mixing panel stay usable on a phone (GDD §13), built now, not deferred. *(Done 04-07-2026:)*
  - [x] Two-layer composite live with a palette-correct **CSS-placeholder master scene** (wall/window/shelf/tap/one counter) — swaps for the locked master scene image at Milestone 8 with no layout changes
  - [x] Programmatic torso-base anchoring: `Stage.composite()` (one counter-line Y + one anchor X per orientation), per-sprite **measured** figure extents in `data/cast.js`, anchor equation verified exact in-browser; standardised contact-shadow ellipse
  - [x] **17 no-bar cutouts keyed, QA'd and shipped** via a purpose-built hue-key pipeline (green-dominance key + despill + luminance overshoot control + watermark/ghost blob cleanup + faint-residue pass); full-res masters filed in `Art/Expressions/Cutouts (No-Bar)/`
  - [x] Dialogue bubble (speaker colour + tail) over the character; mixing-panel frame placeholder with the (i)→Tome button; dev-bar character/expression pickers
  - [x] Mobile-portrait reflow: design space swaps to 720×1280, serve stacks scene → character-at-counter → panel; title/herald/settings restyled for portrait
  - [ ] Visit-beat wiring (beats → `renderServe`) lands with Night 1 content in Milestone 4
  - **Asset flags for Tessa:** Knight **Arrival** has no no-bar render; P.E.K.K.A **Shy** + **Arrival** renders have a real (non-green) counter baked in and couldn't key — outputs set aside in `Art/Expressions/Cutouts (No-Bar)/_review/`, both need re-renders with the green counter; 3 Hog Rider sprites carry a minor olive green-bounce tint on the gold armbands (usable; optional re-render with the anti-bounce line). Wizard robe canon is **blue** per the locked V3 — the batch-QA "red robes" flags were a bad checklist note, not an art problem.
- [x] **3. Brew system.** Three slots (base + two mixers), six spells with canon icons, four live meters (Warmth/Kick/Chill/Bite), dose slider (1–3), tankard recipe indicator with ✓ named-brew check, Reset, Pour → brew animation strip (Pour/Spell Drop/Stir panels) → resume dialogue. Six named recipes wired from data. *(Done 04-07-2026: `src/brew.js`, panel + strip ported from the mockup into the composite, data-driven from SPELLS/RECIPES, labels via the string tables in all 7 languages; all six recipes + dose gating verified in-browser; `Brew.pour(onDone)` hands `{base, mix, dose, recipe}` to the caller — Milestone 4's wrong-brew "gentle beats" hook in there. "Resume dialogue" lands with real beats in M4. Spell/base/recipe names stay English until content localisation (M7 note below).)*
- [x] **4. Dialogue system + Night 1 content.** Real Night 1 script loaded from data; speaker sprites with swappable expressions; tone-shaping choices; wrong-brew "gentle beat" reactions. No fail state. *(Done 04-07-2026: `src/dialogue.js` + Night 1 transcribed VERBATIM into `data/nights.js` from the Hour 1 Script (Draft 2). Cold open → Knight (brew tutorial) → Hog Rider → Wizard (three-way choice replies) → Close → end-of-night Herald. Choices record tones into the save (v2, with Tome unlocks: 3 recipes, 3 Ledger pages, Herald Ed. 1). Full playthrough verified in-browser incl. a deliberate wrong pour. Notes: (a) stage directions render as soft italics inside lines — kept verbatim, not cut; (b) the script has NO bespoke wrong-brew reaction lines — a wrong pour repeats the request line verbatim (gentle, no fail); the engine plays `wrong:[…]` entries the moment Tessa writes them; (c) `[SFX]`/`[ART]` cues are preserved in data and skipped until Milestone 7; (d) one authorial parenthetical ("however Sage plays it") was dropped as a script mechanic, and Knight's Close expression reuses Warm & Content as the script gives no cue — both for Tessa's review.)*
- [x] **5. The Tome (four tabs).** Brew Book (recipes + "taught by"), Regulars' Ledger (pages unlock on meeting; locked silhouettes; printed bio + Sage's margin notes from GDD §10, verbatim), Arena Herald (per-Night editions + ripple marker), Bard's Songbook (four slots: "Sage's Favourite", "Rain on the Strip", "The Little One's Tune", "Last Call" — earned/locked). *(Done 05-07-2026: `src/tome.js` on the mockup's hotspot geometry over the Tome Spread art; full §10 catalogue in `data/tome.js` with marked phrases highlighted and margin notes in the green Caveat hand — printed epigraphs corrected to body serif per §11. Everything gated by the save's unlocks, verified fresh vs Night-1-complete. Ledger stages 2–3 in data, gated until Nights 2–3 (M6). Songbook pick persists via prefs. Portrait falls back to a cream book panel. M7 polish flagged: arrows-to-circled-phrases marginalia per the Ledger Page Mockup; per-language content.)*
- [x] **6. Nights 2 and 3.** Remaining scripts, P.E.K.K.A/Princess features, group + split layouts for the finale, the one consequential brew (Knight, Night 3) → epilogue Herald line. *(Done 05-07-2026: both Nights transcribed VERBATIM from the Hour 2/3 scripts — every visit, choice, reply and Tome cue. Nights 2–3 open on their Herald interstitial (editions archive into the Tome the moment they're shown). New screens: **group** (Layout 3 — the rally: five cutouts torso-anchored to one counter line, standardised shadows, bubble over the speaker, end figures clamped inside the frame) and **split duologue** (Layout 5 — P.E.K.K.A + Hog side by side: diagonal split, bottom-centre name-box in the speaker's colour, silent half dims) — both reflow in portrait. **The consequential brew** (GDD §8): the Knight's last cup accepts any pour, stores clear/rattled in the save (v3), and prints exactly one line in the epilogue Herald + the Tome's Ed. L with the ⚗ ripple marker. Engine growth: brew gates take `any:[…]` (the Wizard's player's-read serve) and `wrongIf:[{has:"Zap"…}]` (P.E.K.K.A's flinch — the script's one bespoke wrong-pour beat, live); Ledger unlocks carry a `stage:` slot → per-character page depth (`ledgerStage(who)`), idempotent under beat replay; save v3 (stage arrays + consequence), v2 saves hydrate. Verified end-to-end in-browser: scripted full playthrough of both Nights incl. the wrong Zap pour, both epilogue variants, Songbook/Herald/Ledger gating, portrait reflow, v2 hydration, clean console.)*
- [x] **7. Audio, localisation scaffolding, polish.** *(Done 06-07-2026.)* Audio system wired (`src/audio.js`): Songbook music channel + brew-fizz/mug-clink/page-turn SFX, pref-gated, graceful missing-file notes — real tracks/SFX drop in later (§16), zero code. Per-Night light shift (§8): programmatic scene tint N1 warm → N2 dusk → N3 candlelight, under the UI. **Canon terms follow Supercell (§13):** Elixir, Dark Elixir + the six spells use the official CR localisation — 42/48 verified with sources, 6 flagged-and-English (audit table in the notes); `canonTerm()` routes the panel + Brew Book. About/Credits + the 7-language string system were already in (M1). Recipe/track names + narrative content stay English until content localisation (a later content pass — the SYSTEM is done). Showstopper dose-2 synced (v1.3).
- [ ] **8. Asset integration.** Final art per GDD §15: **one locked master scene** (bar baked in, brewing shelf/tap/window in place, Scene-B tone — generated once, never regenerated) and the **no-bar chest-up cutouts** keyed along the true silhouette (hands + tankard fully intact — never a flat horizontal crop) from `Art/Incoming - No-Bar Renders (2026-07-04)`. ~~Transparent logo~~ *(done 04-07, Milestone 1)*. **Compress web-served copies of all assets** (the raw AI renders are 2–8 MB each; the title bg went 7.2 MB → 233 KB with no visible loss — do the same for every sprite/scene the game ships).

## Acceptance criteria (GDD §14)

Runs in a browser and on a phone; loading screen animates (no static plate, no blue behind the logo); full 3-Night playthrough with saves; brews compute correctly, ✓ on valid named recipes; wrong brews never block; Tome fills in as you play; Knight's Night 3 brew changes exactly one epilogue line; **one** counter on screen; About screen carries the disclaimer.

## Changes & notes

- **07-07-2026 — Playtest Round 3 complete (build v16): two P0s + reopened #11 + Master Scene V2 + staged Tome reveal + bubble pagination.** Tessa's third playtest on v15. **P0-1 (Save & Quit → Continue reset):** reproduced with real clicks — the save stored only `beatIndex`, never the within-beat position, so Continue replayed the whole visit from line 1 (an early beat ⇒ "the beginning"). Fixed with a **within-beat resume cursor** — `Dialogue.cursor()` persists the remaining queue (current entry re-included) in save **v5**, `Dialogue.resume()` rebuilds the beat, `Game.startBeat()` resumes instead of restarting when the cursor matches; already-consumed lines/choices/pours aren't re-fired, unlocks stay idempotent. Verified mid-conversation / mid-brew / reload→Continue / v4-fallback. **P0-2 (mixer picker off-screen):** `.picker` now opens **upward** + `max-height`/scroll; all six spells reachable both orientations. **#11 (expression swaps, reopened):** the v10 "non-reproducing" verdict was withdrawn — real defect was a choice-reply expr swap **leaking into the trunk** (Wizard puff→Boastful bled into the bare closing line). Fixed with a **branch-scoped restore-sentinel** (fires only on choice-reply expr swaps — the sole case in the script is the Wizard N2 puff branch; every persistent swap is a trunk line and is untouched) + authored the closing line `expr:"Warm Smile"`; an adversarial verifier caught that a blanket `e.expr||beat.expr` revert would break P.E.K.K.A's persistent Calm→Shy, so that was avoided. Proven with a 6-face contact sheet. **Bubble pagination (§11):** a bubble never exceeds ~4 lines; long lines split at sentence boundaries into pages advanced by the same tap+▼, scripts verbatim, stage directions kept whole (Knight two-kings speech → 3 pages, no longer covers his face). **Tome staged reveal (§11 reworked):** the icon doesn't exist until the first recipe records → settles in top-right on a **cream/gilt plate** below the menu button, held glow + tap-first chip, next open on the Brew Book; Knight's first Ledger page glows it again → next open highlights the Ledger ribbon; then normal pulse. Reveal derived from the save (survives reload); chips tap-first ×7 langs; the old bottom-right/above-panel Tome placement is retired. **Master Scene V2 (tall counter, §15):** `#ms-img`/`#g-img` → the V2 webp (counter top 71.9%→**66.7%**); `counterY` re-based to 0.667/0.427 in `stage.js` + `screens.js`; the existing per-expression `drop`s already normalise every mug to one contact line (uniform 496px on the rig), so the cast plants on the deeper band with wood below — V1 spill gone. Four captures re-shot on v16. Files: `src/{dialogue,state,screens,hud,stage}.js`, `data/nights.js`, `css/main.css`, `index.html`, `strings/strings.js`, `assets/scenes/Serve Screen - Master Scene V2 (tall counter).webp` (new). Console clean across a full integrated drive. Round 4 resumes at the Princess's Bittersweet.
- **07-07-2026 — Playtest Round 2 complete (builds v10→v15): system layer + master-scene finish + panel/float/bubble fixes.** The full second-playtest arc: **v10** the 8 approved wrong-pour lines (each hints the right cup, GDD §7 "rejection is the tutorial") + the #9 bubble-tail seam (tail overlaps 2px → one cream silhouette); **v11** the in-game menu subsystem (`src/hud.js` — top-right menu → pause tray Resume·The Tome·Settings·Save & Quit; bottom-right Tome button that pulses on a new unlock, T-key; autosave quill flourish; named Continue "Continue — Night N: Title"; two onboarding chips; continue ▼ in the waiting bubble; §11); **v12** the menu tray's 6-language localisation; **v13/v14** the rebuilt Playwright capture rig (`game/tools/capture.py`, native design scale, no letterboxing, dev-only — exclude `tools/` from deploy) + the **#7 float fix** (per-expression `drop` in `data/cast.js`, position-only, plants every mug on one contact line — measured off the rig, proven with the contact sheet) + the **#8 long-bubble clamp** (pins at `top:8`, grows downward). **v15 (this commit `000fb94`):** **#10 dose-slider own row** (panel restructured — `.panel-cols` wraps the 3 columns, dose is a full-width row beneath, hidden while brewing; before/after proven on the rig); the **per-Night master-scene accents restored** as `#ms-window-fx` (soft-light window-cool) + `#ms-tap-fx` (screen-blend tap-glow) ramping per `#stage.night2/night3`; the **group/rally screen swapped onto the baked master scene** (`#g-img` cover, cast re-anchored counterY 0.719 landscape / 0.46 portrait + the `drop`, N3 brightness/saturation accent); then the **four captures re-shot** (serve-screen, serve-portrait, group-rally, split-duologue) into `../captures/`. **#11 (expression swaps) verified non-reproducing — no code change** (don't "fix" working code). Files across the round: `src/{hud,boot,dialogue,state,screens}.js`, `css/main.css`, `index.html`, `strings/strings.js`, `data/{nights,cast}.js`, `tools/capture.py`. Push to `origin/main` blocked by the auto-mode classifier (wants a feature branch/PR) — commit is safe locally, needs an interactive push. Round closes when Tessa plays the v15 stamp.
- **06-07-2026 — P0 REOPENED + playtest round (build v9): gate-aware ✓, no-silent-rejection, dose drops, Sage bubble, master scene, build stamp.** Tessa's "correct pour also stalls" was reproduced in a real browser as the GDD §7 UX trap, NOT a code stall: at the Knight's dose-1 gate a **valid-but-wrong** Elixir+Heal **dose 2** (*The Easy One*) showed "**The Easy One ✓**", and on pour the panel reset and the SAME request line reappeared with no acknowledgement — indistinguishable from a freeze; the unlabelled Gentle↔Strong slider made pouring 2-drops-while-meaning-1 easy (confirmed: a genuinely-correct dose-1 pour records + resumes fine). Fixes, all verified in real Chrome (dose-1 Usual ticks + records + resumes; dose-2 Easy One shows the name with NO tick; a wrong pour shows the Knight's visible "that's not the usual" line; full 3-Night unscripted playthrough to the epilogue "clear", console clean): **(Fix 2 — gate-aware ✓, GDD §7)** `brew.js` gained an `activeOrder` predicate + `setOrder()`; `update()` names any valid recipe but ticks ONLY when the mix matches the current order; `dialogue.js` `orderMatcher(gate)` reuses `pourMatches` (tick and accept are one rule), armed on the gate dispatch and cleared via `setOrder(null)` on every gate-clear path + `step()`-exhaust + `start()`; the **consequence brew arms a never-true predicate** → name shown, never a tick (no §8 leak). **(Fix 3 — no silent rejection, GDD §7)** the `onPour` else-branch now ALWAYS shows a line — priority `wrongIf` flinch > the beat's spoken `wrong:` line > a generic gentle beat (`brew.wrong.generic`, `{they}` pronoun from `cast.js`); **8 DRAFT `wrong:` lines** wired in `data/nights.js` (marked pending Tessa's word-by-word approval). **(Fix 1 — dose drops)** the slider got • •• ••• detent pips + a live localised "N drop(s)" label (`brew.drops1/2/3` ×7 langs, `updateDoseLabel()` wired into input/reset/refreshText); default stays 1. **(Fix 4 — Sage's bubble)** Sage's spoken lines moved from the top customer bubble (whose tail pointed at the wrong speaker) to a new bottom-left sage-tinted (`#E9EFDC`/`#c6d2b4`) `#sage-bubble` with a deep-sage SAGE label and no tail; the serve adapter forks `who==="Sage"` and the two bubbles mutually hide; portrait rule added. **(Fix 5 — master scene, §15 DONE)** the `#ms` CSS placeholder (wall/window/shelf/tap/counter) is replaced by a single `<img id="ms-img">` of the baked master scene (`Serve Screen - Master Scene V1.png` 2048×1143 → WebP **115 KB** at q86, `object-fit:cover` preserves the 71.9% counter fraction), cast re-anchored `stage.js` counterY **0.755→0.719**; portrait frames the scene into the top 64% band. **Flags:** the per-Night window-cool/tap-glow *accents* lost their CSS hooks (whole-scene `.scene-tint` wash remains — signed-off §8 surface changed); the group/rally screen still uses the `#g-ms` placeholder (serve painted vs group placeholder — follow-up); `captures/serve-screen.png` + `serve-portrait.png` need re-shooting. **(Fix F — build stamp)** `strings.js window.BUILD {n,date,hash}` + `buildLabel()` render a low-contrast `v9 · 06-07` in the title's bottom-right corner (`Screens.fillStatic`), so a player can confirm which build loaded; bumped in lockstep with the `?v=` cache token (now **?v=9**). Files: `src/brew.js`, `src/dialogue.js`, `src/screens.js`, `src/stage.js`, `css/main.css`, `index.html`, `strings/strings.js`, `data/nights.js`, `data/cast.js`, `assets/scenes/Serve Screen - Master Scene V1.webp` (new).
- **06-07-2026 — P0 fix: real-player pour → Tome-record + dialogue-resume stall (reproduced, fixed, verified in a real browser).** Tessa's first real (unscripted) playthrough stalled on Night 1: pouring the Knight's tutorial Usual didn't record into the Tome and progression froze — where every scripted run had passed. **Root cause reproduced:** a stale-cached mismatch (old `state.js` lacking the v4 `poured` field served alongside new `dialogue.js`) made `recordPour` throw `Cannot read properties of undefined (reading 'length')` inside the pour's completion callback, leaving the sequencer stuck in `mode:"brew"` — the recipe never recorded and clicks did nothing. **Fixes (defense-in-depth + the systemic root):** (1) **cache-busting** — every local script/stylesheet now carries `?v=<BUILD>` (`?v=8`); bump the token every build (head-comment in `index.html`) so a refresh always gets the current code (a stale heuristic/`file://` cache burned a real player once). (2) `recordPour` guards a missing/partial `poured` (never throws on an old save-shape). (3) `onPour` clears the gate (`gate=null; Brew.onPour=null; Brew.reset(); mode="idle"`) **before** any side effect and always calls `step()`, in both the normal and the consequential branch — a side-effect throw can no longer freeze the beat; `recordPour` is wrapped in try/catch. (4) `Brew.pour`'s completion runs `onDone` (dialogue resume) **before** the mug-clink SFX, and `Sound.sfx` is wrapped whole so audio can never throw into the brew/flip critical path. (5) **storage-unavailable now says so in-game** (GDD §13): when localStorage is blocked (`file://` / private mode) a cream toast — "This browser can't keep saves — tonight won't be remembered." (localised, all 7 languages) — appears via `Screens.toast()` from `save.js`'s warn hook, instead of a console-only note; the game stays fully playable on the in-memory fallback. **Verified in a real independent Chrome (served origin) — real click events throughout, not scripted engine calls:** correct Usual → ✓ + records (`poured` + Brew Book unlock) + resumes (screenshot); a deliberate wrong pour (Elixir+Rage) → no record, no stall, the Knight re-requests, and a follow-up correct pour recovers; a **mid-Night reload + Continue** lands at the last beat with the Tome intact (screenshot); a full **3-Night unscripted playthrough** reaches the epilogue with the consequential brew's "clear" outcome, all 8 named recipes recorded, console clean (only the expected no-audio-file info notes — no TypeError). Storage-blocked path verified by simulation: the toast fires, the pour still records in-memory and resumes, nothing persists (toast screenshot). `file://` compatibility is architecturally sound (grep-confirmed: zero `fetch()`/ES-modules/`type=module`; classic `window.*` scripts + relative asset paths) — this environment can't drive a literal `file://` session (the Chrome extension forces https and the read-tier blocks desktop clicks), so the `file://`-specific risks were verified via their mechanisms (blocked-storage toast + cache-bust + pour-resume-under-blocked-storage) rather than a driven `file://` session. Files: `index.html`, `src/dialogue.js`, `src/brew.js`, `src/audio.js`, `src/save.js`, `src/screens.js`, `css/main.css`, `strings/strings.js`.
- **06-07-2026 — M7: canon terms follow Supercell (GDD §13; auditable table below).** Elixir, Dark Elixir and the six spells now use the **official Clash Royale localisation**, verified per language by a 54-agent research + adversarial-verify workflow (each term cross-checked against official Supercell localised pages / in-game text CSV / the localised CR Fandom wikis; full per-URL sources in the workflow transcript `wx00864tp`). **42 of 48 verified; the other 6 stay English and are flagged** (never a guessed translation for a canon word — GDD §13). Wired via `canonTerm()` in `strings.js` (splits the `×N` dose suffix; an omitted term falls back to English by design) and routed through the mixing panel + Brew Book; verified in-browser (e.g. de panel renders "Dunkles Elixier / Wut / Knall"; ko "Heal ×2" → "치유 마법 ×2"; the 6 fallbacks render English). Our own coinages (recipe names, track names, Arena Herald, Night Cap) are NOT canon terms — they localise as content later, in-voice.

  **Canon-terms audit** — official CR term per language (`*` = official term is identical to English, verified same, falls back; **EN** = no official term verified → stays English, flagged):

  | Term (EN) | ko | ja | de | es | fr | pt |
  |---|---|---|---|---|---|---|
  | Elixir | 엘릭서 | エリクサー | Elixier | Elixir* | Élixir | Elixir* |
  | Dark Elixir | 다크 엘릭서 | ダークエリクサー | Dunkles Elixier | **EN** | Élixir noir | Elixir Negro |
  | Rage | 분노 마법 | レイジ | Wut | Furia | Rage* | Fúria |
  | Heal | 치유 마법 | ヒール | Heilung | Curación | **EN** | **EN** |
  | Zap | 감전 마법 | ザップ | Knall | Descarga | **EN** | Choque |
  | Freeze | **EN** | フリーズ | Frost | Hielo | Gel | **EN** |
  | Mirror | 반사경 | 鏡 | Spiegel | Espejo | Miroir | Espelho |
  | Poison | 독 마법 | ポイズン | Gift | Veneno | Poison* | Veneno |

  **Stays English (6) — RESOLVED (Tessa delegated → Cowork ruling, 08-07 R13):** per the locked §13 rule an unverified canon word is NEVER guessed; these stay English, the in-product AI-translation disclaimer covers it, and any is patchable post-launch by adding its `term.<name>` key once a native speaker confirms the official Supercell term. Audit closed. The six: ko Freeze · es Dark Elixir · fr Heal · fr Zap · pt Heal · pt Freeze. **Sources (representative; per-term URLs in the `wx00864tp` transcript):** official Supercell localised blog/support pages (Elixir, Dark Elixir, and several spells across ko/ja/de/es/fr), Supercell in-game text CSV (ko/de Rage & Heal via `smlbiobot/cr-csv`), and the localised Clash Royale Fandom / wikiclashroyale.fr / namu.wiki / wikiwiki.jp / deckshop card pages for the spell names — each confirmed by an independent verify agent. *(Note the ko spells carry the in-game "마법 / spell" suffix — 분노 마법 etc. — verbatim per §13; ja Mirror is 鏡, the one native-word spell among ja's transliterations, verified from 3 JA sources.)*
- **06-07-2026 — M7: audio system wired (GDD §14.7/§16; `src/audio.js`).** A `Sound` module (named `Sound`, not `Audio` — the native ctor) with a looping **music channel** (the Bard's Songbook pick: switches on selection, gated by the Music pref, retries a blocked autoplay on the first user gesture) and one-shot **SFX** (brew fizz on pour, mug clink on serve, page turn on a Tome flip), gated by the SFX pref. Boot syncs it to the saved prefs + Songbook pick; Settings' Music toggle drives play/pause; the Songbook pick calls `setTrack`. **Real audio files land later (§16 — not a blocker):** with empty `SONGBOOK[].file` slots it plays silently but every hook works. Verified: no errors; boot defaults to the first track; `setTrack` switches; a fake file proved the `src` wiring (`assets/audio/<file>`); pref gating on both channels; the three SFX triggers all fire. **NEEDS TESSA (M7 audio drop):** the looping tavern-ambient track(s) → `game/assets/audio/` with the filename set in `data/tome.js` `SONGBOOK[].file`; the three SFX → `game/assets/audio/sfx/{brew-fizz,mug-clink,page-turn}.mp3`.
- **06-07-2026 — Milestone 7 start: per-Night light shift (GDD §8).** A subtle programmatic scene tint (never regenerated art) shifts the mood per Night: N1 warm gold (base, no tint) → N2 dusk (a soft-light blue wash; the window cools further) → N3 late-night candlelight (a deeper purple wash, the wall dimmed, the tap-glow intensified). A `.scene-tint` overlay soft-lights the scene AND the character (cohesive lighting) but sits UNDER the UI (panel, bubbles, choices) so text stays readable; driven by `#stage.night{N}` set from `Game.state.night` in `state.js` `applyNightTint()` (called at the top of `playBeat`). Verified across all three Nights (serve screen) via computed styles + rendered frames, and that the real `playBeat` path tags the stage correctly. Works for serve/group/duo; carries over to the locked master scene at M8 with no layout change. *(M7 remaining: audio system wiring, content-localisation scaffolding, About/Credits polish.)*
- **06-07-2026 — Night Cap v2 (composes ON the scroll) + share plumbing + two signed-off-surface fixes (GDD §11/§12 updates).** **(1) Ledger grid regression fixed** — the 12-card pagination had silently replaced the signed-off 5-card layout; restored to a fixed 3-col grid that fills the LEFT page first (uniform cells, top-aligned), spills right, and paginates only past the spread's real capacity (18). Five cards render identical to the signed-off single page; verified at 5/9/10/12/19. Added the **regression-surface rule** to CLAUDE.md. **(2) Back-to-grid** swapped from the ink glyph to the locked **Card Select icon** (fanned sage cards, gold emboss) on the paper with a soft shadow + hover/press lift. **(3) Night Cap v2** now composes on `Night Cap Scroll (cutout).png` over a night `#1C1B3A` backdrop + Elixir-glow vignette (never a bare transparent PNG). Content fills the flat paper between the rolls (safe zone x 16.5–83.5%, y 21.5–80% of the scroll): bigger logo · auto-fitting "Night N — [title]" · date · gold rule · Tonight's Regulars card row · **Poured Tonight as a CENTRED tankard row with names beneath** (mirrors the regulars row, replacing v1's left-aligned list + dead middle) · larger quote (speaker in their colour) · Sage's green flourish · URL at the paper's bottom. Verified Night 2 + Night 3 (spoiler-safe: N3 drinks exclude the consequential cup, quote is the Wizard's) at 100% AND downscaled to 480×600 (feed size) — text survives both. Re-captured `../captures/night-cap.png`. **(4) Share plumbing** — og:image + twitter:card (`summary_large_image`) + og/twitter title/description on **both** `game/index.html` and `site/index.html`, using a pre-rendered Night Cap PNG (`assets/share/night-cap-share.png` / `site/assets/img/night-cap-share.png`); favicon + apple-touch-icon generated from the tankard icon. **FLAG: absolute og:url + og:image URLs get set on deploy** (part of the canonical-URL swap, alongside the Night Cap footer `GAME_URL`). **Asset note:** the scroll web copy is 1.6 MB (on-demand only) — add to the M8 compression sweep.
- **06-07-2026 — the Night Cap share card (GDD §11 v1.4; `src/nightcap.js`).** At each Night's end ("Keep Tonight's Page" on the nightend screen for Nights 1–2 and the epilogue for Night 3) a 1200×1500 parchment postcard composes on a `<canvas>` from shipped art only — logo, "Night N — [title]", the real localised date, the Night's MET visitors as mini card portraits (cropped to their measured content box), the drinks poured as tankard icons + recipe names, ONE curated spoiler-safe quote (Cormorant, speaker name in their §12 colour), Sage's green Caveat "Same time tomorrow?" (the ONLY green), and the game URL. PNG download + Web Share API (download fallback). **Spoiler-safety is structural:** the consequential brew is NEVER logged to the save (`state.poured`, added in `dialogue.recordPour`), so it can't reach the drinks list; the quote is curated per Night and the Night 3 quote is the Wizard's, not the Knight/outcome. Save bumped to **v4** (`poured:[{night,recipe}]`, deduped); v2/v3 hydrate `poured` as `[]`. Verified in-browser: EN + JA composes (CJK renders via system fallback — see the font note), spoiler-safety (N3 excludes the consequential + night-filters drinks), untainted-canvas PNG (2.2 MB, `toBlob` works offline), Web-Share fallback, save hydration. A 7-agent adversarial review (4 dimensions → verify) confirmed 2 issues, both fixed: canvas fonts now carry generic fallbacks so non-Latin chrome renders in a system font not tofu; `toBlob` guards a null blob and `download`/`share` have `.catch`. Capture: `../captures/night-cap.png` (Night 2 — The Draw).
- **06-07-2026 — arrow system round 7 (semantics; `src/tome.js` + `#tomenav`).** The fletched arrow read as a "duplicate" because back-to-grid AND prev-sheet were the same left-pointing icon on one page. Now the fletched arrow is RESERVED for page-turns — prev/next on the character spread AND the (new) paginated grid, rendered only when that neighbouring sheet exists (prev never on a first sheet). **Back-to-grid is a distinct round parchment button** with an ink card-grid glyph (gilt/ink UI, never green), top-left in the same reserved paper box, only on a spread. **Grid pagination built now** (future-proofing past ten cards): the grid flows two pages per spread and paginates with the same fletched prev/next at the bottom corners. All nav furniture lives in one `#tomenav` overlay (same rect as `#spread`), cleared on every render so nothing lingers. Verified with rendered frames + numeric checks: spread sheet-0 (grid button + next, no prev), continuation sheet (grid button + prev, visually distinct), grid after return (zero strays), a simulated 12-card grid turning to its second sheet (next on sheet 0, prev on sheet 1).
- **06-07-2026 — landing page Milestone 1 (`site/`, Launch Pack Brief §2).** Scrollytelling product page started under `site/` — separate from the game, ships alongside it so "Play" is one click. M1 = **chapter 1 hero** (the game's own title scene: dimmed bar backdrop + the real Elixir-mote particle drift ported from `src/particles.js`, transparent logo, tagline, Play CTA, tankard scroll cue that tips) and **chapter 2 "the bar builds itself"** (a pinned scene that assembles the painted bar on scroll — room warms → purple taps ignite → amber lanterns bloom staggered → the Knight settles at the counter → his real Night-1 line "Evening. You're new here, then." pops). Dependency-free by design (matches the game): classic scripts on `window.*`, self-hosted fonts, native scroll + `position:sticky` driving ONE `--p` custom property per scene (no GSAP, no scroll-jacking), transforms/opacity only; `prefers-reduced-motion` **and** no-JS collapse the pins to composed frames (engine-owned `html.js` flag). Web copies of logo/bar/knight/tankard compressed into `site/assets/img/` (bar 498→141 KB; the PNGs still heavy — M3); fonts copied from `assets/fonts`. **Verified by computed-style sampling** — this session's preview screenshot pipeline was unavailable (headless viewport reported 0×0), so pixel placement of the Knight/bubble/glows is best-estimate pending a real viewport or Tessa's eye. A 4-lens adversarial review (guard-rails / CSS / a11y / fidelity) ran; M1 findings fixed: lazy-load ch2 images, engine-owned `.js` flag (no-JS truly falls back), CSS-guaranteed reduced-motion collapse, amber focus ring, AA speaker-label contrast (`--knight-ink`), marginalia corrected to match taps-first order, disclaimer/alt/copy tidied. Preview served locally via a dependency-free PowerShell static server (`.claude/launch.json` "site", session scratchpad — not committed). **M2 next:** chapters 3–8 (regulars rail, the brew, tomorrow's news, the Tome, play-it, one-bar-every-arena) + full footer. **M3 backlog (mobile + reduced-motion + perf):** portrait framing (the 16:9 stage collapses to a thin band on phones — crop the bar into a taller portrait frame + re-anchor the glow/char %s), make the Knight's entry slide proportional (not a fixed 130px), quantise the heavy PNGs (knight-arrival 843 KB, logo 465 KB) + preload tankard, Lighthouse mobile ≥90, per-character on-cream label colours for the ch3 cast.
- **05-07-2026 — page-turn round 6 (placement only; turn geometry untouched).** Round 5's "arrows outside the text viewport" had pushed them into the OUTER margin — which is the page-edge stack / leather, not paper. Fixed to sit ON the flat paper of the page each acts on: back = left page paper top-left (stage 15.6%/10.5%), prev = left page paper bottom-left (19.4%/85.3%), next = right page paper bottom-right (80.1%/85.3%) — each clear of the page-edge stack, gutter and leather. The no-overlap guarantee is kept by RESERVING the arrow bands in the layout: `#spreadvp` now ends at 79.6% (a 5% bottom reserve) so flowed text/notes stop well above the bottom arrows (82.5%), and the back arrow sits entirely above the viewport. Portrait (cream panel, no stack) reserves top+bottom bands and keeps the arrows inside the panel corners. Verified with rendered frames (not just numbers): Princess stage-3 spread + its continuation sheet + a stage-1 spread — every arrow fully on paper, clear of the card, bio and Sage's notes, with measured margins (back 1%+ past the stack, prev ~5%, next ~2% inside the right stack).
- **05-07-2026 — launch-pack captures shipped** (Launch Pack Brief session 2). Six deliverables in `../captures/` (workshop folder, not this repo): `serve-screen.png` (Wizard + The Showstopper ✓), `group-rally.png` (all five, Knight's rally), `split-duologue.png` (P.E.K.K.A + Hog, Layout 5), `epilogue-herald.png` (Ed. L with the ripple line), `serve-portrait.png` (phone serve, P.E.K.K.A + The Gentle One ✓), `page-turn.gif` (27 frames, grid→Knight spread turn) — all pixel-exact 1280×720 (portrait 720×1280), rendered at native design scale (no upscaling). The landing page (brief §2) and case study pull from there.
- **05-07-2026 — page-turn FINAL round (round 5) — SHIPPED: the 3D leaf. The crossfade fallback was NOT needed.** Tessa's two remaining faults from the frame-level review, both fixed and verified: **(1) Nav arrows now live in RESERVED zones** — all three sit OUTSIDE the spread's text viewport, in the art's painted page margins: exactly one back-to-grid arrow, fixed at the top-left outer margin; prev/next only at the bottom OUTER corners. Because `#spreadvp` clips every flowed word and margin note, no layout outcome can ever put content under an arrow (the Princess stage-3 pinned-note collision from the video is structurally impossible now). Portrait keeps them in the clear bands above/below the panel. **(2) The leaf can no longer rise above the page block — geometrically.** The perspective eye now sits level with the leaf's TOP edge (`perspective-origin: 49.82% 7.2%`, lens 4200px): a point level with the eye never moves vertically under projection and every leaf point is below it, so no rotation about the spine (turn, lag, curl) can push any corner above the block on any frame — this holds identically for BOTH turn types, which share the one `flipTo()` geometry. The longer lens keeps the free edge's downward growth inside the stage. **(3) Bonus fault found in the captured frames:** the REV leaf's incoming face was sliced past the right page's paper edge into the painted ribbon tabs — mid-arc the sheet carried little ribbon chips. A parchment `edgemask` now covers that far strip in flight and fades out just before touchdown, so the landed frame stays pixel-identical to the art. **Verified as demanded:** 10 timeline seeks × both directions × both turn types on the stage-3 Princess spread (max rise above the page-top line 0.01px, hinge ≤0.01px, leaf fully inside the stage; full-bleed art backdrop confirmed on both types), plus ACTUAL captured frames via a visible browser tab: fwd 25/50/75%, rev 50%, and the arrow layout on both sheets — including the exact pinned-note scene from the video, now clean.
- **05-07-2026 — Milestone 6: Nights 2 & 3 playable — build calls for Tessa's review.** Reconciliations and judgement calls made while transcribing (all flagged, none silent): **(1) "The Gentle One" is now the seventh recipe** (Elixir + Heal, dose 3). The GDD §7 table lists six and gives Elixir + Heal ×2 to The Easy One, but §10 and the Hour 2 script record P.E.K.K.A's drink as "The Gentle One" — the scripts win. Dose 3 is a build call (keeps all three Elixir + Heal names dose-distinct: 1 Usual / 2 Easy One / 3 Gentle One — the biggest, slowest pour for the biggest regular); her brew gate accepts any dose so it never plays as a puzzle. **(2) Brew Book copy:** the mockup's "Easy One — for P.E.K.K.A" attribution contradicted the GDD table and Hour 3 (it's the Hog's tournament-eve drink); the P.E.K.K.A copy moved to The Gentle One and the Easy One's new taught/note lines are build-written from his beat — Tessa may want to polish them. **(3)** P.E.K.K.A's Night 3 brew gate reuses her Night 2 order line as the wrong-pour nudge (the Night 3 script gives her no request line). **(4)** Princess swaps to Guard-Down Warmth at "Down here nobody's at the right range…" — GDD §9 assigns the expression to Night 3 but the script has no ART cue placing it. **(5)** The Knight's stage-3 Ledger unlock (empty note — §10's pinned line carries the content) fires at the rally's end. **(6)** Herald edition numbers run sequentially (XLVIII, XLIX, L) from Night 1's locked XLVII. **(7)** The Wizard's "see him" branch serves the player's read — the gate accepts either the Showstopper or the quiet Usual, per the script's "player's read" brew note.
- **05-07-2026 — page-turn round 4 (GDD §11, frame-by-frame review 3 — the three faults fixed).** All three had mechanical causes. (1) *See-through leaf:* the animated `drop-shadow` filter forced the browser to flatten the preserve-3d leaf and smeared a translucent shadow over the gutter — filters are banned from the 3D tree now, and the faces carry an opaque paper colour under their art slice. (2) *Bound edge off the spine:* the `rotateX` wobble rotated the leaf about a horizontal axis, physically lifting the bound edge's top/bottom off the spine plane (and its droop opened the dark gutter-notch gap that read as brown). The leaf + a new nested `.paper` wrapper now rotate ONLY about the spine axis; softness instead comes from the wrapper lagging a few degrees behind the hinge (free edge trails, settles flat — lag held at 0 for the first 12% so the sheet can never dip behind the book plane), free-edge-only corner rounding (was wrongly rounding the spine side of the back face), and the travelling sheen band. (3) *Page-block peeking:* paper in the art was re-measured (x 11.1→84.9%, y 7.7→87.6% — the old leaf's 8.07% top left painted page uncovered) and the leaf now spans the full page-block (top 7.2%, height 81%, fwd width 35.48%) with slices recomputed; the backdrop was already the full-bleed art to every edge. Verified by animation-timeline seeking at 9 points in both directions: hinge gap ≤0.05px throughout, paper band fully covered at every frame, `filter: none`, opaque faces, backface swap at mid-turn, console clean.
- **05-07-2026 — page-turn round 3 (GDD §11, frame-by-frame review 2).** Root cause of "whole spread turning": the perspective eye was centred, so the free edge ballooned toward the camera. Now: eye ON the spine with a 2800px lens — projection measured page-sized across the whole arc (max 1.03× page at touchdown, never past the book edge). Full-bleed static-spread backdrop means the leather can never peek and the book never moves. Paper softness faked: rotateX wobble, free-edge corner radius mid-flight (on the faces — the preserve-3d leaf stays unclipped), travelling sheen/shade bend-band.
- **05-07-2026 — page-turn round 2 + arrow routing (GDD §11, from Tessa's frame-by-frame review).** Turn geometry re-derived from the Tome Spread art itself (pages measured at 11.1–84.7% × 8.1–87.2%, spine 49.8%): the leaf is one full painted page hinged on the spine, art slices on both faces, and opaque art covers occlude both pages until it lands (destination never peeks). Note arrows: nearest-margin placement + short arrow to the phrase's nearest edge, routed through the inter-line gap below the line — never across words (supersedes strict alternation; scatter emerges from phrase positions).
- **05-07-2026 — 3D page-turn (GDD §11).** The flip is now a real turn: a full page at the pages' exact size, hinged at the spine, rotating in 3D (1600px perspective), parchment front/back with backface swap, curl-shadow sweeping the facing page, reversed rotation going back. Verified by driving the animation timeline (0→178° progression) — the hidden preview tab pauses CSS animation playback, so live-frame observation isn't possible headlessly, but the timeline seek proves the motion.
- **05-07-2026 — Ledger spread round 4: true book flow (GDD §11 "reading order and balance").** The spread is one continuous flow across two CSS columns (= the two pages): the bio ALWAYS starts on the left page under the card/nameplate and the browser carries text onto the right page mid-paragraph when needed (fixes the bio-starting-right bug; the balanced block packer is gone). Notes + green arrows moved to a strip-coordinate overlay — alternation, line-level alignment and arrows unchanged. Long entries slide two columns per page-turn; stage-3 entries mostly fit one spread now (denser flow). 3D nav-arrow asset swap picked up (1786×536).
- **05-07-2026 — Ledger spread round 3 (GDD §11 update).** Nav arrows now use the fletched-arrow icon from `Art/UI` (flipped for next-page); pen (✎) glyphs removed from Sage's notes; consecutive notes alternate left/right margins with the alternation threading across the spread; sheets balance their two pages (least weight difference, reading order and capacity preserved) so stage-1 spreads read card+nameplate left, bio right. Note: the nav icon web copy is 676 KB — covered by the M8 compress-web-assets sweep.
- **05-07-2026 — Ledger spread refinements (GDD §11 update).** Generous margins (page padding, 16px/1.52 bio in a centred column with equal rails, flow onto the right page as entries grow). Nav arrows are now ornamental ink+gilt UI — the green hand-drawn style belongs only to Sage's note→phrase arrows. Notes verified level with their marked phrase's line across all five characters (measuring rail at true content width + in-situ re-layout; opposite-rail fallback on collisions; tilts rotation-only).
- **05-07-2026 — Ledger redesigned as a BOOK (GDD §11 rewrite — supersedes the scrollbar/detail-panel notes below).** No scrollbars anywhere in the Tome; pages turn. Grid → click a met card → parchment page-flip to a full two-page character spread: card top-left, nameplate beside it at mid-height (no banner/divider), bio flowing across both pages in a narrow column, Sage's notes in the margins beside their marks with hand-drawn arrows. Back arrow to the grid; overflow paginates to further sheets via hand-drawn arrows (blocks measured and packed at real size). `window.DEV_LEDGER_STAGE = 2|3` previews the later bio stages. Replacement Knight card measured (1525×2048, content aspect 1.371) — all five cards now genuinely uniform.
- **05-07-2026 — Ledger detail-page polish (GDD §11).** Tome scrollbars themed (thin/rounded, parchment + gilt). Marginalia laid out properly: bio in a narrower centre column, each green note in the margin beside its marked phrase with a short hand-drawn arrow (longer notes drop to the foot with an arrow from their mark) — no stacking. Cards clip to measured content boxes (`px` + `box` in `data/tome.js`), so selection outlines hug the art; the re-trimmed Knight card re-measured (1412×2048, symmetric — `cardCX` removed). His content aspect is still 1.46 vs the others' 1.37, so at uniform height he reads narrower — awaiting Tessa's possible re-gen for a perfect aspect match.
- **05-07-2026 — Songbook progression-gated (GDD §11).** Locked tracks are greyed rows revealing nothing (no title/description — no Night-3 spoilers on a fresh save). Declarative `when` schedule in `data/tome.js`, resolved against the save: Sage's Favourite always · Rain on the Strip at end of Night 1 (Herald Ed. 1) · The Little One's Tune on meeting P.E.K.K.A · Last Call at Night 3. Explicit `unlocks.songs` still counts (story unlocks + dev fill-save). "Last Call" by-line reworded off "finale" — the format keeps adding Nights.
- **05-07-2026 — Ledger cards: UNIFORM height (supersedes the varied-heights note below — Tessa corrected the steer; GDD §15 updated).** All five cards normalise by measured content HEIGHT to one common line, aspect preserved (the Knight's taller frame just renders slightly narrower; his one-sided export padding is centre-compensated via `cardCX`). Met-check fixed to key-presence (empty notes no longer read as locked). Dev bar gained **fill save** — populates a fully-unlocked save and opens the Tome for layout review without replaying.
- **05-07-2026 — serve UI: choice block moved bottom-left** (per Tessa's review): tone choices stack up from the bottom-left corner, verified clear of every character's face in both orientations; lower-body/counter overlap is accepted by design.
- **05-07-2026 — regenerated cast swapped in (FINAL art) + Milestone 5.** All 20 no-bg cutouts used as-is (no keying), placement calibrated on a full-cast contact sheet: per-sprite counter anchors at the torso/hand line, one tall factor per character so heads stay constant across expressions (Princess petite, P.E.K.K.A huge — canon staging; Knight Arrival stands 1.17× by design). Old sprite sets retired from the repo. Milestone 5 Tome built on the mockup geometry with the §10 Ledger bios verbatim. `prefs.track` added for the Songbook pick.
- **04-07-2026 — Milestone 4: Night 1 playable.** Dialogue engine (`src/dialogue.js`): click-to-advance lines with expression swaps, italic narration, tone choices with inline reply branches, brew gates hooked to `Brew.onPour`, Tome unlocks applied to the save as they happen. Save format bumped to v2 (`unlocks` + `tones`); v1 saves hydrate cleanly. Herald behaviour per the script: Night 1 cold-opens with no morning paper, and Ed. XLVII lands as the END-of-night interstitial. Content TODOs for Tessa: wrong-brew reaction lines (engine ready), and Nights 2–3 transcription lands at Milestone 6.
- **04-07-2026 — serve-screen review fixes + Milestone 3.** (1) **Contact shadow standardised:** the keying pipeline now strips ALL baked render shadows (soft pixels beyond a feather's width of the silhouette), so the one CSS ellipse — soft, near-black, low-opacity, behind the figure at the counter line — is the single shadow for the whole cast, verified identical across all five. All 17 placeholder cutouts re-processed. (2) **Bubble clearance:** cast height reduced to leave real headroom (landscape 0.58×H, portrait 0.36×H) and the bubble is now bottom-anchored 14px above figure top (which includes P.E.K.K.A's horns) — it can no longer overlap any head. (3) **Current sprites are PLACEHOLDERS** — Tessa is regenerating the full no-bar set (green-bounce fix); re-key via the same pipeline when they land in the incoming folder, do not finalise sprite art until then.
- **04-07-2026 — Milestone 2 serve composite built; 17 sprites keyed.** No Python on this machine, so the keying pipeline was rebuilt as PowerShell + compiled C# (`scratchpad/key-sprite.ps1`, mirrors `Tools/key_asset.py`'s intent): hue-based green key (handles the renders' green counters at any shade), soft-edge despill, luminance overshoot control (neutral contact shadows per GDD §12), watermark-star/ghost blob removal, faint-residue cleanup. Keyed twice through an 18-agent parallel QA loop; every output zoom-checked against the Sprite Spec. CAST expression shape evolved from plain filenames to `{file, top, base}` (measured anchor metadata) — consumers updated. Cast framing proved remarkably consistent: torso-base at 76.8% of frame in 13 of 17 renders.
- **04-07-2026 — Milestone 1 built + Settings (GDD §11 addition) + adversarial review pass.** A 16-agent review (logic / spec / browser-robustness lenses, each finding independently verified) confirmed 6 issues, all fixed: `startNight` night-bounds guard (was able to persist an unresumable save via the dev bar); `prefers-reduced-motion` now honoured live, not just at boot; dead `edition` field removed from the documented herald beat shape; Tome ✕ now via the string table; title-screen payload cut from ~8.5 MB to ~1 MB (bg → 1920×1080 JPEG, logo keyed + optimised — repo keeps only web copies, originals stay in `Art/`); fonts self-hosted (latin woff2, SIL OFL) so the game has zero external dependencies and runs fully offline. Deviations from the letter of the GDD, on purpose: language/sound prefs live under `elixirHour.prefs.v1` beside the save (§11 says "in the save") so New Game never resets your language; UI-chrome strings are AI-translated in all 7 languages now to make the language switch real, while narrative content translates with its content milestones.
- **04-07-2026 — GitHub live; auto-push from now on.** Tessa installed `gh`, created and pushed the `elixir-hour` repo. Standing instruction: after each milestone, commit with a clear message and push automatically — no need to ask.
- **04-07-2026 — sprite question RESOLVED (GDD §12 wins, locked 04-07).** The scene approach is a **two-layer composite**: one master scene with the bar baked in (generated once, never regenerated) + no-bar chest-up cutouts composited on top. Not a separate counter strip; the earlier baked-bar-per-sprite approach is superseded. Cutout & anchor rules now in §12: key the true silhouette (hands/tankard intact, no flat horizontal crops), anchor by torso-base to one fixed counter-line Y + one horizontal anchor for the whole cast, standardised contact shadow — all placed programmatically in code.
- **04-07-2026 — repo created.** Assets copied are the QA-passed set the Serve Screen Mockup references: 20 character cutouts (**old baked-bar versions — placeholders** until the no-bar set from `Art/Incoming - No-Bar Renders (2026-07-04)` is keyed and copied in), Sage anchor, 3 scene plates, 6 UI pieces, 5 cards, logo.
- Knight card in `assets/ui/cards/` is the least-damaged placeholder (ghost watermark stars bottom-right); regen pending — Ledger shows it as locked silhouette until then.
- Logo still has the dark-blue plate; transparent version pending (Milestone 8).
